diff --git a/README.md b/README.md
index 2c69b49..f3ab62e 100644
--- a/README.md
+++ b/README.md
@@ -46,6 +46,28 @@ For the current product presentation, demo and credits rationale, refer to:
 
 https://axiomsphereai.com
 
+## Claude Code Bedrock Launchers
+
+Three Claude Code launchers are provided under `scripts/launchers/` for running Claude Code via AWS Bedrock:
+
+| Script | Model |
+|--------|-------|
+| `aims_claude_opus46.sh` | Claude Opus 4.6 |
+| `aims_claude_sonnet46.sh` | Claude Sonnet 4.6 |
+| `aims_claude_haiku45.sh` | Claude Haiku 4.5 |
+
+Each launcher automatically checks for a valid AWS SSO session and triggers `aws sso login` if the token is expired — no credentials are stored in files. Run from the workspace root:
+
+```bash
+bash scripts/launchers/aims_claude_opus46.sh
+```
+
+Verify all launchers before use:
+
+```bash
+bash scripts/launchers/verify_ai_cli_launchers.sh
+```
+
 ## Public Repository Status
 
 This repository is under review as part of the project's public/private source and licensing boundary. The authoritative public description of AxiOMSphere is the website linked above.
diff --git a/aims_workspace/agent_architecture_status/docsreg_phase6_checkpoint/files_modified.txt b/aims_workspace/agent_architecture_status/docsreg_phase6_checkpoint/files_modified.txt
index ab22b86..3eed0ae 100644
--- a/aims_workspace/agent_architecture_status/docsreg_phase6_checkpoint/files_modified.txt
+++ b/aims_workspace/agent_architecture_status/docsreg_phase6_checkpoint/files_modified.txt
@@ -398,7 +398,7 @@
 ?? docs/AIMS_CODE_SKILLS_CURRICULUM_V1.md
 ?? docs/AIMS_CONTINUOUS_SELF_LEARNING_PIPELINE.md
 ?? docs/AIMS_FULL_FUNCTIONAL_CERTIFICATION_V1.md
-?? docs/AIMS_GSTACK_INTEGRATION_STATUS.md
+?? docs/AIMS_FULLSTACK_INTEGRATION_STATUS.md
 ?? docs/AIMS_HIDDEN_FAILURE_SEARCH_PLAN.md
 ?? docs/AIMS_MODEL_BENCHMARK_POLICY.md
 ?? docs/AIMS_MODEL_CANDIDATE_LIFECYCLE.md
@@ -438,8 +438,8 @@
 ?? docs/agents/AIMS_AUTONOMOUS_DEV_TEAM.md
 ?? docs/agents/AIMS_BOTS_INTERFACES.md
 ?? docs/agents/AIMS_DEVELOPER_PIPELINE_AUDIT.md
-?? docs/agents/AIMS_GSTACK_AGENT_MAPPING.md
-?? docs/agents/AIMS_GSTACK_SKILL_ADAPTATION.md
+?? docs/agents/AIMS_FULLSTACK_AGENT_MAPPING.md
+?? docs/agents/AIMS_FULLSTACK_SKILL_ADAPTATION.md
 ?? docs/agents/axi_logi_project_context_vault_and_aims_guided_delivery_architecture.md
 ?? docs/agents/skills/README.md
 ?? docs/agents/skills/architect_planning.md
@@ -1032,10 +1032,10 @@
 ?? ops/evals/fixtures/
 ?? ops/evals/full_functional_certification/
 ?? ops/evals/full_process_certification/
-?? ops/evals/gstack_agent_integration_audit.py
-?? ops/evals/gstack_e2e_scenario_audit.py
-?? ops/evals/gstack_flow_eval.py
-?? ops/evals/gstack_skill_coverage_audit.py
+?? ops/evals/fullstack_agent_integration_audit.py
+?? ops/evals/fullstack_e2e_scenario_audit.py
+?? ops/evals/fullstack_flow_eval.py
+?? ops/evals/fullstack_skill_coverage_audit.py
 ?? ops/evals/hermes_analysis_validation_smoke.py
 ?? ops/evals/hermes_claim_verification_smoke.py
 ?? ops/evals/hermes_claude_mem_context_locator_smoke.py
@@ -1195,7 +1195,7 @@
 ?? ops/gateway/anthropic_proxy.py.bak_20260503_162725
 ?? ops/gateway/check_omniroute_fallback.sh
 ?? ops/gateway/start_gateway.sh
-?? ops/gstack/
+?? ops/fullstack/
 ?? ops/inbox_cleanup.sh
 ?? ops/inter_agent_exchange/
 ?? ops/iso55001_processes.json
diff --git a/aims_workspace/agent_architecture_status/docsreg_phase7_real_execution/files_modified.txt b/aims_workspace/agent_architecture_status/docsreg_phase7_real_execution/files_modified.txt
index 310eb0e..7452fe4 100644
--- a/aims_workspace/agent_architecture_status/docsreg_phase7_real_execution/files_modified.txt
+++ b/aims_workspace/agent_architecture_status/docsreg_phase7_real_execution/files_modified.txt
@@ -969,7 +969,7 @@
 ?? docs/AIMS_CODE_SKILLS_CURRICULUM_V1.md
 ?? docs/AIMS_CONTINUOUS_SELF_LEARNING_PIPELINE.md
 ?? docs/AIMS_FULL_FUNCTIONAL_CERTIFICATION_V1.md
-?? docs/AIMS_GSTACK_INTEGRATION_STATUS.md
+?? docs/AIMS_FULLSTACK_INTEGRATION_STATUS.md
 ?? docs/AIMS_HIDDEN_FAILURE_SEARCH_PLAN.md
 ?? docs/AIMS_MODEL_BENCHMARK_POLICY.md
 ?? docs/AIMS_MODEL_CANDIDATE_LIFECYCLE.md
@@ -1009,8 +1009,8 @@
 ?? docs/agents/AIMS_AUTONOMOUS_DEV_TEAM.md
 ?? docs/agents/AIMS_BOTS_INTERFACES.md
 ?? docs/agents/AIMS_DEVELOPER_PIPELINE_AUDIT.md
-?? docs/agents/AIMS_GSTACK_AGENT_MAPPING.md
-?? docs/agents/AIMS_GSTACK_SKILL_ADAPTATION.md
+?? docs/agents/AIMS_FULLSTACK_AGENT_MAPPING.md
+?? docs/agents/AIMS_FULLSTACK_SKILL_ADAPTATION.md
 ?? docs/agents/axi_logi_project_context_vault_and_aims_guided_delivery_architecture.md
 ?? docs/agents/skills/README.md
 ?? docs/agents/skills/architect_planning.md
@@ -1571,10 +1571,10 @@
 ?? ops/evals/fixtures/
 ?? ops/evals/full_functional_certification/
 ?? ops/evals/full_process_certification/
-?? ops/evals/gstack_agent_integration_audit.py
-?? ops/evals/gstack_e2e_scenario_audit.py
-?? ops/evals/gstack_flow_eval.py
-?? ops/evals/gstack_skill_coverage_audit.py
+?? ops/evals/fullstack_agent_integration_audit.py
+?? ops/evals/fullstack_e2e_scenario_audit.py
+?? ops/evals/fullstack_flow_eval.py
+?? ops/evals/fullstack_skill_coverage_audit.py
 ?? ops/evals/hermes_analysis_validation_smoke.py
 ?? ops/evals/hermes_claim_verification_smoke.py
 ?? ops/evals/hermes_claude_mem_context_locator_smoke.py
@@ -1734,7 +1734,7 @@
 ?? ops/gateway/anthropic_proxy.py.bak_20260503_162725
 ?? ops/gateway/check_omniroute_fallback.sh
 ?? ops/gateway/start_gateway.sh
-?? ops/gstack/
+?? ops/fullstack/
 ?? ops/inbox_cleanup.sh
 ?? ops/inter_agent_exchange/
 ?? ops/iso55001_processes.json
diff --git a/aims_workspace/agent_architecture_status/docsreg_phase8_checkpoint/files_modified.txt b/aims_workspace/agent_architecture_status/docsreg_phase8_checkpoint/files_modified.txt
index 1cff6ca..8fe14c2 100644
--- a/aims_workspace/agent_architecture_status/docsreg_phase8_checkpoint/files_modified.txt
+++ b/aims_workspace/agent_architecture_status/docsreg_phase8_checkpoint/files_modified.txt
@@ -998,7 +998,7 @@
 ?? docs/AIMS_CODE_SKILLS_CURRICULUM_V1.md
 ?? docs/AIMS_CONTINUOUS_SELF_LEARNING_PIPELINE.md
 ?? docs/AIMS_FULL_FUNCTIONAL_CERTIFICATION_V1.md
-?? docs/AIMS_GSTACK_INTEGRATION_STATUS.md
+?? docs/AIMS_FULLSTACK_INTEGRATION_STATUS.md
 ?? docs/AIMS_HIDDEN_FAILURE_SEARCH_PLAN.md
 ?? docs/AIMS_MODEL_BENCHMARK_POLICY.md
 ?? docs/AIMS_MODEL_CANDIDATE_LIFECYCLE.md
@@ -1038,8 +1038,8 @@
 ?? docs/agents/AIMS_AUTONOMOUS_DEV_TEAM.md
 ?? docs/agents/AIMS_BOTS_INTERFACES.md
 ?? docs/agents/AIMS_DEVELOPER_PIPELINE_AUDIT.md
-?? docs/agents/AIMS_GSTACK_AGENT_MAPPING.md
-?? docs/agents/AIMS_GSTACK_SKILL_ADAPTATION.md
+?? docs/agents/AIMS_FULLSTACK_AGENT_MAPPING.md
+?? docs/agents/AIMS_FULLSTACK_SKILL_ADAPTATION.md
 ?? docs/agents/axi_logi_project_context_vault_and_aims_guided_delivery_architecture.md
 ?? docs/agents/skills/README.md
 ?? docs/agents/skills/architect_planning.md
@@ -1600,10 +1600,10 @@
 ?? ops/evals/fixtures/
 ?? ops/evals/full_functional_certification/
 ?? ops/evals/full_process_certification/
-?? ops/evals/gstack_agent_integration_audit.py
-?? ops/evals/gstack_e2e_scenario_audit.py
-?? ops/evals/gstack_flow_eval.py
-?? ops/evals/gstack_skill_coverage_audit.py
+?? ops/evals/fullstack_agent_integration_audit.py
+?? ops/evals/fullstack_e2e_scenario_audit.py
+?? ops/evals/fullstack_flow_eval.py
+?? ops/evals/fullstack_skill_coverage_audit.py
 ?? ops/evals/hermes_analysis_validation_smoke.py
 ?? ops/evals/hermes_claim_verification_smoke.py
 ?? ops/evals/hermes_claude_mem_context_locator_smoke.py
@@ -1763,7 +1763,7 @@
 ?? ops/gateway/anthropic_proxy.py.bak_20260503_162725
 ?? ops/gateway/check_omniroute_fallback.sh
 ?? ops/gateway/start_gateway.sh
-?? ops/gstack/
+?? ops/fullstack/
 ?? ops/inbox_cleanup.sh
 ?? ops/inter_agent_exchange/
 ?? ops/iso55001_processes.json
diff --git a/docker-compose.dgx.yml b/docker-compose.dgx.yml
index e0c4622..8c144e8 100644
--- a/docker-compose.dgx.yml
+++ b/docker-compose.dgx.yml
@@ -23,6 +23,7 @@ services:
       context: /home/axi_omi_sphere/aims-workspace
       dockerfile: Dockerfile.scheduler
     image: axiomsphere-scheduler:local
+    restart: always
     volumes:
       - /var/run/docker.sock:/var/run/docker.sock
       # Отчёты omi_night_smoke: на хосте см. aims_workspace/scheduler_logs/night_tests.log
diff --git a/docker-compose.yml b/docker-compose.yml
index 38de60a..e5d4abf 100644
--- a/docker-compose.yml
+++ b/docker-compose.yml
@@ -48,6 +48,7 @@ services:
       AXI_FT_LOG_ENABLED: '1'
       AXI_FT_LOG_DIR: /data/axi_ft_log
       AXI_LLM_MAX_CONCURRENT: '1'
+      AWS_PROFILE: ${AWS_PROFILE:-AdministratorAccess-445100240501}
       
             # OmniRoute teacher combo integration
       OMNIROUTE_BASE_URL: http://host.docker.internal:20128/v1
@@ -129,6 +130,9 @@ services:
     - type: bind
       source: /home/axi_omi_sphere/aims-workspace/data/docs_agent
       target: /data/docs_agent
+    - type: bind
+      source: /home/axi_omi_sphere/.aws
+      target: /root/.aws
     extra_hosts:
     - host.docker.internal=host-gateway
   inbox-cleanup:
@@ -244,7 +248,8 @@ services:
   omi-bot:
     profiles:
     - telegram-bots
-    mem_limit: 512m
+    mem_limit: 2g
+    stop_grace_period: 120s
     build:
       context: /home/axi_omi_sphere/aims-workspace
       dockerfile: ops/Dockerfile.omi-bot
@@ -275,7 +280,7 @@ services:
       OMI_ALLOWED_CHAT_IDS: -5266680971,-1003609238392,-1003745546504
       OMI_ALLOWED_USER_IDS: '8077374184,8003780560'
       OMI_GROUP_UPLOAD_NOTIFICATIONS: "0"
-      OMI_GROUP_SILENT_MODE: "1"
+      OMI_GROUP_SILENT_MODE: "0"
       OMI_DB_PATH: /data/aims_registry.db
       OMI_LLM_BACKEND: httpx
       # omi_bot.py reads TELEGRAM_BOT_TOKEN — override with OMI_BOT_TOKEN so it doesn't get the axi-bot token
@@ -311,6 +316,7 @@ services:
       AIMS_REDIS_URL: redis://aims-redis:6379
       # Бэкапы БД: /data/backups → AIMS_BACKUP_HOST_DIR на хосте Spark. Для ПК Andrei: sshfs (ops/scripts/mount_andrei_aims_backup.sh), затем compose up.
       AIMS_BACKUP_DIR: /data/backups
+      AWS_PROFILE: ${AWS_PROFILE:-AdministratorAccess-445100240501}
     depends_on:
       aims-redis:
         condition: service_healthy
@@ -342,6 +348,9 @@ services:
     - type: bind
       source: /home/axi_omi_sphere/aims-workspace/ops
       target: /ops
+    - type: bind
+      source: /home/axi_omi_sphere/.aws
+      target: /root/.aws
     working_dir: /omi_telegram
   omi-quality-gate:
     command:
diff --git a/docs/agents/skills/logi_strategy.md b/docs/agents/skills/logi_strategy.md
index 501636f..8967813 100644
--- a/docs/agents/skills/logi_strategy.md
+++ b/docs/agents/skills/logi_strategy.md
@@ -17,6 +17,9 @@ Operational rules:
 - Do not execute runtime/service changes.
 - Do not invoke destructive commands (no destructive operations, readonly for all runtime).
 - Keep strategy output traceable and explicit.
+- Convert ambiguous requests into an executable spec before delegation: objective, scope, constraints, success criteria, agent routing, required evidence, and protected actions.
+- Save and restore project context through AIMS handoff/evidence artifacts; do not rely on external memory services or raw upstream runtime state.
+- Treat any mutation, deployment, service change, training launch, browser automation, or external-control step as a protected action requiring an explicit request artifact.
 
 Workflow:
 1. Capture owner intent.
@@ -32,3 +35,13 @@ Required outputs:
 - `03_scope_options.md`
 - `04_decision_log.json`
 - `05_handoff_plan.json`
+- `context_save.json`
+- `context_restore_notes.md`
+- `protected_action_request.json` when the plan needs approval-gated work
+
+Fullstack provenance aliases:
+- `aims-fullstack-context-save`
+- `aims-fullstack-context-restore`
+- `aims-fullstack-spec`
+
+These are aliases/provenance records only. The active execution surface is this AIMS Logi strategy pack.
diff --git a/ops/agents/skills/section_editor.py b/ops/agents/skills/section_editor.py
index ce15e13..a6d5464 100644
--- a/ops/agents/skills/section_editor.py
+++ b/ops/agents/skills/section_editor.py
@@ -32,11 +32,35 @@ from concurrent.futures import ThreadPoolExecutor, as_completed
 from dataclasses import dataclass, field
 sys.path.insert(0, os.path.join(os.path.dirname(__file__), "..", "..", ".."))
 from ops.cyclic_skills import validate_structure
+from ops.models.model_registry import resolve_slot
+from ops.agents.skills.context_grounded_document_generation import (
+    match_reference_section,
+)
 
 log = logging.getLogger("section_editor")
+EDITOR_MODEL = resolve_slot("32")
 
-OLLAMA_URL   = "http://127.0.0.1:11434"
-SLOT120      = "axi_omi_sphere"  # SLOT32 (32B) is optimal for section editing
+
+def _ollama_base_url() -> str:
+    try:
+        from ollama_resolve import effective_ollama_base_url
+
+        return effective_ollama_base_url().rstrip("/")
+    except Exception:
+        return os.environ.get("OLLAMA_BASE_URL", "http://127.0.0.1:11434").rstrip("/")
+
+# Regex patterns for standards NOT in the AIM-PFM reference (same list as Phase 3).
+# Used by _verify_recommendation to confirm deletions actually happened.
+_FABRICATED_STANDARDS_RE = re.compile(
+    r"API\s+(?:510|570|580|581|RP\s*571|RP\s*572|RP\s*574)"
+    r"|NACE\s+SP\d{4}"
+    r"|IEC\s+60364"
+    r"|NFPA\s+70"
+    r"|ASME\s+B31\.\d"
+    r"|AS\s*4024"
+    r"|BS\s*EN\s+ISO\s+14001",
+    re.IGNORECASE,
+)
 
 # ── Data structures ──────────────────────────────────────────────
 
@@ -73,31 +97,86 @@ class SectionEditResult:
 
 # ── Ollama call ──────────────────────────────────────────────────
 
+_OLLAMA_RETRY_BACKOFF = (0, 15, 30)  # seconds before attempt 1, 2, 3
+
+
+def _ollama_response_schema(prompt: str) -> dict:
+    if '"revised_body"' in prompt:
+        return {
+            "type": "object",
+            "additionalProperties": False,
+            "required": ["section_id", "revised_body", "applied", "skipped"],
+            "properties": {
+                "section_id": {"type": "string"},
+                "revised_body": {"type": "string", "minLength": 1},
+                "applied": {"type": "array", "items": {"type": "string"}},
+                "skipped": {"type": "array", "items": {"type": "string"}},
+            },
+        }
+    return {
+        "type": "object",
+        "additionalProperties": False,
+        "required": ["section_id", "body", "applied"],
+        "properties": {
+            "section_id": {"type": "string"},
+            "body": {"type": "string", "minLength": 1},
+            "applied": {"type": "array", "items": {"type": "string"}},
+        },
+    }
+
+
+def _ollama_health_ping() -> bool:
+    """Return True if Ollama /api/tags responds within 5 seconds."""
+    import urllib.request
+    try:
+        with urllib.request.urlopen(f"{_ollama_base_url()}/api/tags", timeout=5):
+            return True
+    except OSError:
+        return False
+
+
 def _ollama(prompt: str, timeout: int = 300) -> str:
+    import time
     import urllib.request
     payload_obj = {
-        "model": SLOT120,
-        "prompt": (
-            "<|im_start|>system\nYou are a precise document editor. "
-            "Always respond with valid JSON only.<|im_end|>\n"
-            f"<|im_start|>user\n{prompt}<|im_end|>\n"
-            "<|im_start|>assistant\n<think>\n\n</think>\n\n"
+        "model": EDITOR_MODEL,
+        "system": (
+            "You are a precise document editor. "
+            "Return only JSON matching the requested schema."
         ),
-        "raw": True,
+        "prompt": prompt,
+        "format": _ollama_response_schema(prompt),
         "stream": False,
         "options": {"temperature": 0.10, "num_predict": 4096},
     }
     raw = ""
     for _attempt in range(3):
+        backoff = _OLLAMA_RETRY_BACKOFF[_attempt]
+        if backoff:
+            log.warning("[OLLAMA] Backing off %ds before attempt %d/3...", backoff, _attempt + 1)
+            time.sleep(backoff)
+            if not _ollama_health_ping():
+                log.warning("[OLLAMA] Health ping failed on attempt %d/3 — Ollama may be loading model", _attempt + 1)
         payload = json.dumps(payload_obj).encode()
         req = urllib.request.Request(
-            f"{OLLAMA_URL}/api/generate",
+            f"{_ollama_base_url()}/api/generate",
             data=payload,
             headers={"Content-Type": "application/json"},
             method="POST",
         )
-        with urllib.request.urlopen(req, timeout=timeout) as r:
-            raw = json.loads(r.read().decode()).get("response", "").strip()
+        try:
+            with urllib.request.urlopen(req, timeout=timeout) as r:
+                raw = json.loads(r.read().decode()).get("response", "").strip()
+        except (json.JSONDecodeError, ValueError) as parse_err:
+            # Ollama returned empty or non-JSON body (queue contention / internal error)
+            log.warning("[OLLAMA] Parse error on attempt %d/3: %s — retrying...", _attempt + 1, parse_err)
+            raw = ""
+            continue
+        except OSError as conn_err:
+            # socket.timeout, ConnectionRefusedError, URLError — Ollama unreachable
+            log.warning("[OLLAMA] Connection error on attempt %d/3: %s — retrying...", _attempt + 1, conn_err)
+            raw = ""
+            continue
         # Strip think blocks
         raw = re.sub(r"<think>.*?</think>\s*", "", raw, flags=re.DOTALL)
         if "<think>" in raw:
@@ -424,6 +503,17 @@ REFERENCE DOCUMENT — this section in the source standard (use exact terminolog
 {reference_section}
 """
 
+    # Phase 3 fabricated-standards deletion recs use "STEP 1 — DELETE" language.
+    # The "Preserve all existing standards references" rule directly contradicts them
+    # and causes the model to output nothing at low temperature. Omit it when present.
+    has_deletion_recs = any(
+        ("STEP 1" in r.raw and "DELETE" in r.raw) for r in recs
+    )
+    preserve_standards_rule = (
+        "" if has_deletion_recs
+        else "- Preserve all existing standards references\n"
+    )
+
     return f"""You are editing one section of a professional Oil & Gas Asset Integrity Management document.
 
 DOCUMENT OUTLINE (TOC):
@@ -449,7 +539,7 @@ RULES:
 - Do NOT add placeholder text like "TBD" or "(to be defined)"
 - For ADD_CONTENT/EXPAND/UPDATE: expand or improve existing content using REFERENCE DOCUMENT content
 - For REPLACE: rewrite the section completely based on REFERENCE DOCUMENT and recommendations
-- Preserve all existing standards references
+{preserve_standards_rule}
 
 RESPOND WITH THIS EXACT JSON STRUCTURE:
 {{"section_id": "{entry.section_id}", "revised_body": "...", "applied": {json.dumps(rec_ids)}, "skipped": []}}"""
@@ -518,6 +608,18 @@ def _verify_recommendation(
     if revised_body.strip() == original_body.strip():
         return False
 
+    # Phase 3 deletion recs: confirm fabricated standards were actually removed.
+    # The prompt uses "STEP 1 — DELETE" language; trust the outcome, not the model's
+    # self-report — it often adds ISO content alongside API refs instead of removing.
+    if "STEP 1" in rec.raw and "DELETE" in rec.raw:
+        remaining = _FABRICATED_STANDARDS_RE.findall(revised_body)
+        if remaining:
+            log.debug(
+                "[VERIFY] Deletion rec %s failed outcome check — still contains: %s",
+                rec.rec_id, remaining,
+            )
+            return False
+
     for criterion in rec.criteria:
         if "table" in criterion and not re.search(
             r"(?m)^\s*\|.+\|\s*$", revised_body
@@ -873,8 +975,11 @@ def _call_section(
         rollback = True
 
     # Check 2: not shorter than original (no shrinkage)
-    if len(revised_body.split()) < len(entry.body.split()) * 0.7:
-        log.warning("[EDITOR] Section %s shrank >30%% — rolling back", entry.section_id)
+    # Threshold 0.5: Phase 3 standards-deletion edits can legitimately shrink by ~40-50%
+    # when removing fabricated API/NACE/ASME citations; Phase 1 structural edits expand
+    # content so they are unaffected by this threshold.
+    if len(revised_body.split()) < len(entry.body.split()) * 0.5:
+        log.warning("[EDITOR] Section %s shrank >50%% — rolling back", entry.section_id)
         rollback = True
 
     reason = ""
@@ -930,6 +1035,62 @@ def _call_section(
     )
 
 
+def _call_section_atomic(
+    entry: SectionEntry,
+    recs: list[NormalizedRec],
+    toc: str,
+    catalog: list[SectionEntry],
+    reference_section: str = "",
+) -> SectionEditResult:
+    """Apply one recommendation per model call and commit each verified change."""
+    current = entry
+    applied: list[str] = []
+    skipped: list[str] = []
+    verified: list[str] = []
+    reasons: list[str] = []
+
+    for rec in recs:
+        result = _call_section(
+            current,
+            [rec],
+            toc,
+            catalog,
+            reference_section,
+        )
+        applied.extend(result.applied)
+        if result.status == "COMMITTED":
+            verified.extend(result.verified)
+            current = SectionEntry(
+                section_id=current.section_id,
+                heading=current.heading,
+                level=current.level,
+                start=current.start,
+                end=current.end,
+                body=result.revised_body,
+            )
+        else:
+            skipped.append(rec.rec_id)
+            if result.reason:
+                reasons.append(f"{rec.rec_id}: {result.reason}")
+
+    if verified:
+        status = "COMMITTED"
+        reason = "; ".join(reasons)
+    else:
+        status = "ROLLED_BACK"
+        reason = "; ".join(reasons) or "no_atomic_edit_verified"
+
+    return SectionEditResult(
+        section_id=entry.section_id,
+        revised_body=current.body if verified else entry.body,
+        applied=list(dict.fromkeys(applied)),
+        skipped=list(dict.fromkeys(skipped)),
+        verified=list(dict.fromkeys(verified)),
+        status=status,
+        reason=reason,
+    )
+
+
 # ── Step 5: Parallel execution from one snapshot ─────────────────
 
 def _can_run_parallel(sections: list[str], catalog: list[SectionEntry]) -> list[list[str]]:
@@ -1103,8 +1264,21 @@ def apply_section_edits(
                 entry = next((e for e in catalog if e.section_id == sid), None)
                 if not entry:
                     continue
-                ref_sec = _extract_reference_section(reference_text, sid) if reference_text else ""
-                futures[ex.submit(_call_section, entry, groups[sid], toc, catalog, ref_sec)] = sid
+                ref_sec = (
+                    match_reference_section(reference_text, entry.heading)
+                    if reference_text
+                    else ""
+                )
+                futures[
+                    ex.submit(
+                        _call_section_atomic,
+                        entry,
+                        groups[sid],
+                        toc,
+                        catalog,
+                        ref_sec,
+                    )
+                ] = sid
 
             for future in as_completed(futures):
                 sid = futures[future]
diff --git a/ops/argus/argus_bot.py b/ops/argus/argus_bot.py
index 202c250..925ad60 100644
--- a/ops/argus/argus_bot.py
+++ b/ops/argus/argus_bot.py
@@ -137,6 +137,7 @@ from ft_lifecycle import (
     version_paths as ft_version_paths,
 )
 import argus_diagnose as _diag
+from core.runtime_names import QUEUE_PENDING, QUEUE_PROCESSING
 
 # ── Phase 5: EventBus bridge (Logi orchestration) ──────────────────────────────
 try:
@@ -1999,6 +2000,27 @@ def _tasks_fetch(stuck_only: bool) -> list[dict]:
     return []
 
 
+def _tasks_cancel(task_id: str, reason: str = "cancelled by operator") -> tuple[bool, str]:
+    """Cancel a task in task-registry."""
+    try:
+        r = httpx.patch(
+            f"{TASK_REG_URL}/tasks/{task_id}/cancel",
+            json={"reason": reason},
+            timeout=5.0,
+        )
+        if r.status_code == 200:
+            data = r.json()
+            return True, f"task_id={data.get('task_id', task_id)} cancelled"
+        try:
+            body = r.json()
+            return False, body.get("error", f"HTTP {r.status_code}")
+        except Exception:
+            return False, f"HTTP {r.status_code}"
+    except Exception as exc:
+        log.debug("tasks_cancel: %s", exc)
+        return False, str(exc)
+
+
 def _fmt_tasks(rows: list[dict], stuck_only: bool) -> str:
     if not rows:
         return "Нет задач ✅" if stuck_only else "Реестр пуст"
@@ -2073,11 +2095,17 @@ async def cmd_help(update: Update, ctx: ContextTypes.DEFAULT_TYPE) -> None:
         "/unload &lt;model&gt; [node] — выгрузить из VRAM\n"
         "/pull &lt;model&gt; [node] — скачать модель\n"
         "/delete &lt;model&gt; [node] — удалить с диска узла\n\n"
+        "<b>Задачи</b>\n"
+        "/task_cancel &lt;task_id&gt; [reason] — отменить task registry task\n\n"
         "<b>FT (fine-tuning)</b>\n"
         "/ft &lt;14|32|70|72&gt; — проверить готовность тюнинга и ночной шаг\n"
         "/ft &lt;14|32|70|72&gt; run — запустить сейчас (после подтверждения кнопкой)\n"
         "/ft log [14|70|72|qwen3] — хвост лога тренировки\n"
         "/ft download — прогресс загрузок HF-моделей\n\n"
+        "<b>Slot14 lifecycle</b>\n"
+        "/slot14 report — eval report + delete request + PC pin decision\n"
+        "/slot14 cleanup — only cleanup request for losers\n"
+        "/slot14 pin — current PC Andrei pin decision\n\n"
         "<b>Eval и деплой</b>\n"
         "/eval &lt;candidate&gt; [baseline] — A/B сравнение по golden_v2 suite\n"
         "/deploy &lt;model&gt; [andrei] — перенести модель с DGX на PC Andrei\n\n"
@@ -2470,6 +2498,26 @@ async def cmd_tasks(update: Update, ctx: ContextTypes.DEFAULT_TYPE) -> None:
     )
 
 
+async def cmd_task_cancel(update: Update, ctx: ContextTypes.DEFAULT_TYPE) -> None:
+    if not _allowed(update):
+        return
+    if not ctx.args:
+        await update.message.reply_text(
+            "Использование: /task_cancel <task_id> [reason]",
+            parse_mode=ParseMode.HTML,
+        )
+        return
+    task_id = ctx.args[0].strip()
+    reason = " ".join(ctx.args[1:]).strip() or "cancelled by operator"
+    await update.message.chat.send_action(ChatAction.TYPING)
+    ok, msg = await asyncio.to_thread(_tasks_cancel, task_id, reason)
+    icon = "✅" if ok else "❌"
+    await update.message.reply_text(
+        f"{icon} cancel <code>{_h(task_id)}</code>\n<pre>{_h(msg[:400])}</pre>",
+        parse_mode=ParseMode.HTML,
+    )
+
+
 async def cmd_scheduler(update: Update, ctx: ContextTypes.DEFAULT_TYPE) -> None:
     """Show Redis Scheduler live dashboard: daemon, container, queues, last tasks."""
     if not _allowed(update):
@@ -3275,6 +3323,60 @@ async def cmd_eval(update: Update, ctx: ContextTypes.DEFAULT_TYPE) -> None:
     await update.message.reply_text(body, parse_mode=ParseMode.HTML)
 
 
+# ── /slot14 lifecycle ────────────────────────────────────────────────────────
+
+async def cmd_slot14(update: Update, ctx: ContextTypes.DEFAULT_TYPE) -> None:
+    """Slot14 multi-model report / cleanup request / pin decision."""
+    if not _allowed(update):
+        return
+
+    args = [a.strip().lower() for a in (ctx.args or []) if a.strip()]
+    mode = args[0] if args else "report"
+    if mode not in {"report", "cleanup", "pin", "emit"}:
+        await update.message.reply_text(
+            "Использование:\n"
+            "  /slot14 report   — full report + cleanup request + pin decision\n"
+            "  /slot14 cleanup  — only cleanup request for losers\n"
+            "  /slot14 pin      — only pin decision",
+            parse_mode=ParseMode.HTML,
+        )
+        return
+
+    await update.message.chat.send_action(ChatAction.TYPING)
+
+    def _run() -> tuple[bool, str]:
+        proc = subprocess.run(
+            [
+                "python3",
+                "ops/traini/slot14_lifecycle_report.py",
+                "emit" if mode in {"report", "emit"} else mode,
+                "--pc-url",
+                os.environ.get("PC_ANDREY_OLLAMA_URL", "http://10.77.77.2:11434"),
+            ],
+            cwd="/workspace",
+            capture_output=True,
+            text=True,
+            timeout=180,
+        )
+        stdout = (proc.stdout or "").strip()
+        stderr = (proc.stderr or "").strip()
+        combined = "\n".join(filter(None, [stdout, stderr])).strip()
+        return proc.returncode == 0, combined or "(no output)"
+
+    ok, body = await asyncio.to_thread(_run)
+    icon = "✅" if ok else "❌"
+    title = {
+        "report": "Slot14 report",
+        "emit": "Slot14 report",
+        "cleanup": "Slot14 cleanup request",
+        "pin": "Slot14 pin decision",
+    }.get(mode, "Slot14 report")
+    await update.message.reply_text(
+        f"{icon} <b>{title}</b>\n<pre>{_h(body[:3500])}</pre>",
+        parse_mode=ParseMode.HTML,
+    )
+
+
 # ── /deploy ───────────────────────────────────────────────────────────────────
 
 async def cmd_deploy(update: Update, ctx: ContextTypes.DEFAULT_TYPE) -> None:
@@ -4927,7 +5029,7 @@ async def _on_chat_message_locked(
             "dgx": cmd_dgx, "wake": cmd_wake, "sleep": cmd_sleep,
             "digest": cmd_digest, "plan": cmd_plan,
             "ft_log": cmd_ft_log, "ft_download": cmd_ft_download,
-            "eval": cmd_eval, "deploy": cmd_deploy,
+            "eval": cmd_eval, "deploy": cmd_deploy, "slot14": cmd_slot14,
         }
         _handler = _cmd_dispatch.get(_intent_cmd)
         if _handler:
@@ -5488,6 +5590,8 @@ async def _post_init(app: Application) -> None:
         BotCommand("digest",    "Утренний дайджест прямо сейчас"),
         BotCommand("train",     "Обучение моделей: статус датасета / запуск"),
         BotCommand("ft",        "FT skill: 14/32/70/72 (check + run now)"),
+        BotCommand("slot14",    "Slot14 eval report / cleanup request / pin decision"),
+        BotCommand("task_cancel", "Отменить task registry задачу"),
         BotCommand("code",      "Контроль кода: status/review"),
         BotCommand("help",      "Справка"),
     ])
@@ -5534,6 +5638,7 @@ def main() -> None:
     app.add_handler(CommandHandler("pull",      cmd_pull))
     app.add_handler(CommandHandler("delete",    cmd_delete))
     app.add_handler(CommandHandler("tasks",     cmd_tasks))
+    app.add_handler(CommandHandler("task_cancel", cmd_task_cancel))
     app.add_handler(CommandHandler("scheduler", cmd_scheduler))
     app.add_handler(CommandHandler("incidents", cmd_incidents))
     app.add_handler(CommandHandler("diagnose",  cmd_diagnose))
@@ -5552,6 +5657,7 @@ def main() -> None:
     app.add_handler(CommandHandler("train",     cmd_train))
     app.add_handler(CommandHandler("ft",        cmd_ft))
     app.add_handler(CommandHandler("eval",      cmd_eval))
+    app.add_handler(CommandHandler("slot14",    cmd_slot14))
     app.add_handler(CommandHandler("deploy",    cmd_deploy))
     app.add_handler(CommandHandler("code",      cmd_code))
     app.add_handler(MessageHandler(filters.TEXT & ~filters.COMMAND, on_chat_message))
diff --git a/ops/argus/argus_eventbus_bridge.py b/ops/argus/argus_eventbus_bridge.py
index 81b3280..722ae4a 100644
--- a/ops/argus/argus_eventbus_bridge.py
+++ b/ops/argus/argus_eventbus_bridge.py
@@ -16,23 +16,27 @@ import os
 from datetime import datetime
 from typing import Optional
 
-# EventBus imports (lazy)
-_event_bus = None
+# EventBus imports
 _event_bus_enabled = os.environ.get("AIMS_PHASE5_EVENTBUS_ENABLED", "true").lower() in ("true", "1", "yes")
 
 
 async def get_event_bus():
-    """Get or initialize EventBus (lazy load)."""
-    global _event_bus
-    if _event_bus is None and _event_bus_enabled:
-        try:
-            from logi.event_bus import get_bus
-            _event_bus = await get_bus(
-                redis_url=os.environ.get("REDIS_URL", "redis://localhost:6379")
-            )
-        except Exception as e:
-            print(f"⚠ EventBus initialization failed: {e}")
-    return _event_bus
+    """Get EventBus singleton from event_bus module.
+
+    Uses the singleton pattern in event_bus.py to guarantee the same
+    instance is returned on all calls.
+    """
+    if not _event_bus_enabled:
+        return None
+
+    try:
+        from ops.logi.event_bus import get_bus
+        return await get_bus(
+            redis_url=os.environ.get("REDIS_URL", "redis://localhost:6379")
+        )
+    except Exception as e:
+        print(f"⚠ EventBus initialization failed: {e}")
+        return None
 
 
 async def publish_monitor_event(
diff --git a/ops/argus/argus_orchestrator.py b/ops/argus/argus_orchestrator.py
index 4d88912..388396e 100644
--- a/ops/argus/argus_orchestrator.py
+++ b/ops/argus/argus_orchestrator.py
@@ -20,14 +20,16 @@ Env vars:
 """
 from __future__ import annotations
 
+import asyncio
 import json
 import logging
 import os
 import subprocess
 import threading
 import time
+import uuid
 from dataclasses import dataclass, field
-from datetime import datetime, timedelta
+from datetime import datetime, timedelta, timezone
 from pathlib import Path
 from typing import Any, Callable
 import sys
@@ -77,6 +79,15 @@ except ModuleNotFoundError:
 
 log = logging.getLogger("argus.orchestrator")
 
+# ── Scheduler imports (graceful degradation if scheduler not deployed) ──────────
+try:
+    import redis.asyncio as _aioredis
+    from ops.scheduler.task_scheduler import RedisQueueManager
+    from ops.scheduler.task_models import TaskMetadata, TaskStatus
+    _SCHEDULER_AVAILABLE = True
+except ImportError:
+    _SCHEDULER_AVAILABLE = False
+
 # ── Config ─────────────────────────────────────────────────────────────────────
 PLANS_DIR = Path(os.environ.get("ARGUS_PLANS_DIR", "/workspace/ops/argus/plans"))
 DIGEST_HOUR = int(os.environ.get("ARGUS_DIGEST_HOUR", "7"))
@@ -88,6 +99,46 @@ CATCH_UP_HOURS = int(os.environ.get("ARGUS_CATCH_UP_HOURS", "20"))
 CATCH_UP_MAX_PER_TICK = max(1, int(os.environ.get("ARGUS_CATCH_UP_MAX_PER_TICK", "3")))
 CATCH_UP_STARTUP_GRACE_SEC = max(0, int(os.environ.get("ARGUS_CATCH_UP_STARTUP_GRACE_SEC", "300")))
 
+# ── schedule_task action — security policy ────────────────────────────────────
+_ALLOWED_TASK_TYPES: frozenset = frozenset({
+    "training_ingest",
+    "training_generate_pairs",
+    "ft_prepare_chain_run",
+    "daily_deploy_14b",
+    "docbench_nightly",
+    "argus_smoke",              # Safe no-op for E2E scheduler validation
+    "slot14_nightly_eval",      # Nightly eval for SLOT14 model
+    "slot32_nightly_eval",      # Nightly eval for SLOT32 model
+    "omi_docs_standards_dry_run",  # Omi: dry-run standards ingest
+    "job_filter_nightly",       # Job filter: nightly batch scan
+    "vram_unload",              # Pre-docbench VRAM unload
+    # ── Logi tasks (Phase 4 wiring) ──────────────────────────────────────
+    "logi_cross_dept_sync",     # Cross-department AIMS sync (lightweight)
+    "logi_review_cycle",        # Claude review cycle batch (lightweight)
+    "logi_planned_action_gpu",  # Live-gpu-gated planned action (slot32)
+    # ── Omi tasks (Phase 4 full adoption) ────────────────────────────────
+    "omi_ocr_pipeline_run",     # OCR pipeline: doc → registry ingest
+    # ── Doci tasks (BP-12 adoption) ──────────────────────────────────────
+    "doci_doc_generation_batch",  # Batch document generation (CPU-only, lightweight)
+    # ── Knomi tasks (BP-13 adoption) ─────────────────────────────────────
+    "knomi_reindex",              # Scheduled/nightly vector index rebuild (qdrant writes)
+})
+_GATED_TASK_TYPES: frozenset = frozenset({
+    "slot120_train",
+    "slot120_eval",
+})
+_CMD_ALLOWED_PREFIXES: tuple = ("python", "bash", "pytest")
+_CMD_FORBIDDEN_PATTERNS: tuple = (
+    "rm -rf",
+    "git push --force",
+    "git reset --hard",
+    "kill",
+    "shutdown",
+    "reboot",
+    "curl",
+    "wget",
+)
+
 # ── Cron helpers ───────────────────────────────────────────────────────────────
 def _cron_field_matches(field: str, value: int) -> bool:
     if field == "*":
@@ -383,6 +434,7 @@ def _exec_run_script(params: dict) -> tuple[bool, str]:
     if not script:
         return False, "no script specified"
     workspace = os.environ.get("AIMS_WORKSPACE", "/workspace")
+    timeout_seconds = int(params.get("timeout_seconds", 300) or 300)
     try:
         # Backward-compatible mode:
         # - if `script` points to an existing file:
@@ -397,13 +449,13 @@ def _exec_run_script(params: dict) -> tuple[bool, str]:
         result = subprocess.run(
             cmd,
             cwd=workspace,
-            capture_output=True, text=True, timeout=300,
+            capture_output=True, text=True, timeout=timeout_seconds,
         )
         ok = result.returncode == 0
         out = (result.stdout + result.stderr)[:400]
         return ok, out.strip()
     except subprocess.TimeoutExpired:
-        return False, "script timed out (300s)"
+        return False, f"script timed out ({timeout_seconds}s)"
     except Exception as e:
         return False, str(e)
 
@@ -431,9 +483,12 @@ class ArgusOrchestrator:
         on_notify: Callable[[str], None] | None = None,
         on_approval_request: Callable[[PlanStep], None] | None = None,
         storage: "ArgusStorage | None" = None,
+        on_missed_start_report: Callable[[list], None] | None = None,
     ) -> None:
         self._on_notify = on_notify or (lambda _msg: None)
         self._on_approval_request = on_approval_request or (lambda _step: None)
+        # Phase 3: async-safe callback for missed-startup task report (button message)
+        self._on_missed_start_report = on_missed_start_report or (lambda _tasks: None)
         self._storage = storage
         self._state = OrchestratorState(STATE_FILE)
         self._steps: list[PlanStep] = []
@@ -452,6 +507,7 @@ class ArgusOrchestrator:
         self._catchup_attempted: dict[str, datetime] = {}
         self._catchup_lock = threading.Lock()
         self._started_at: datetime | None = None
+        self._event_loop: asyncio.AbstractEventLoop | None = None
 
     def reload_plans(self) -> int:
         self._steps = _load_plans(PLANS_DIR)
@@ -474,17 +530,26 @@ class ArgusOrchestrator:
     # ── MAIN LOOP ─────────────────────────────────────────────
 
     def _loop(self) -> None:
+        """Background daemon thread: owns the persistent asyncio event loop."""
+        loop = asyncio.new_event_loop()
+        asyncio.set_event_loop(loop)
+        self._event_loop = loop
+        try:
+            loop.run_until_complete(self._async_loop())
+        finally:
+            self._event_loop = None
+            loop.close()
+
+    async def _async_loop(self) -> None:
         last_minute = -1
-        while not self._stop.wait(timeout=15):
+        while not self._stop.is_set():
             now = datetime.now()
+            if now.minute != last_minute:
+                last_minute = now.minute
+                await self._tick(now)
+            await asyncio.sleep(15)
 
-            if now.minute == last_minute:
-                continue
-
-            last_minute = now.minute
-            self._tick(now)
-
-    def _tick(self, now: datetime) -> None:
+    async def _tick(self, now: datetime) -> None:
         # 1. обычные шаги
         for step in self._steps:
             if not step.cron:
@@ -504,12 +569,12 @@ class ArgusOrchestrator:
 
             log.info("plan step fired: %s", step.id)
             self._state.mark_run(step.id, now)
-            self._fire(step, now, is_catchup=False)
+            await self._fire(step, now, is_catchup=False)
 
         # 2. catch-up
-        self._run_catchups(now)
+        await self._run_catchups(now)
 
-    def _execute_action(self, step: PlanStep) -> tuple[bool, str]:
+    async def _execute_action(self, step: PlanStep) -> tuple[bool, str]:
         action = step.action
         if action == "create_task":
             return _exec_create_task(step.params)
@@ -517,14 +582,115 @@ class ArgusOrchestrator:
             return _exec_run_script(step.params)
         if action == "notify":
             return _exec_notify(step.params)
+        if action == "schedule_task":
+            return await self._exec_schedule_task(step.params)
         if action == "send_digest":
             return True, "digest"
         log.warning("unknown action: %s", action)
         return False, f"unknown action: {action}"
 
+    async def _exec_schedule_task(self, params: dict) -> tuple[bool, str]:
+        """Enqueue a task into the Redis scheduler.
+
+        Argus enqueues only — does NOT execute commands directly.
+        Flow: validate params → build TaskMetadata → persist hash → enqueue.
+        RedisQueueManager.schedule_task() publishes TASK_SCHEDULED internally;
+        Argus must NOT publish it again.
+        """
+        if not _SCHEDULER_AVAILABLE:
+            return False, "schedule_task: scheduler not available (import failed)"
+
+        # ── 1. Required params ─────────────────────────────────────────────
+        task_type = params.get("task_type", "")
+        command = params.get("command", [])
+        scheduled_for = params.get("scheduled_for", "")
+
+        if not task_type:
+            return False, "schedule_task: missing required param 'task_type'"
+        if not command:
+            return False, "schedule_task: missing required param 'command'"
+        if not scheduled_for:
+            return False, "schedule_task: missing required param 'scheduled_for'"
+        if not isinstance(command, list):
+            return False, f"schedule_task: 'command' must be a list, got {type(command).__name__}"
+
+        # ── 2. Task-type allowlist ─────────────────────────────────────────
+        if task_type not in _ALLOWED_TASK_TYPES and task_type not in _GATED_TASK_TYPES:
+            return False, f"schedule_task: task_type '{task_type}' not in allowlist"
+
+        # ── 3. Gated training policy ───────────────────────────────────────
+        if task_type in _GATED_TASK_TYPES and not params.get("gated_training_approved"):
+            return False, (
+                f"schedule_task: task_type '{task_type}' requires "
+                "'gated_training_approved: true' in params"
+            )
+
+        # ── 4. Command validation ──────────────────────────────────────────
+        cmd_str = " ".join(str(c) for c in command)
+        if not any(cmd_str.startswith(p) for p in _CMD_ALLOWED_PREFIXES):
+            return False, (
+                f"schedule_task: command must start with one of "
+                f"{_CMD_ALLOWED_PREFIXES}, got: {cmd_str[:80]}"
+            )
+        for forbidden in _CMD_FORBIDDEN_PATTERNS:
+            if forbidden in cmd_str:
+                return False, f"schedule_task: forbidden pattern '{forbidden}' in command"
+
+        # ── 5. Timestamp validation ────────────────────────────────────────
+        try:
+            ts_norm = scheduled_for.replace("Z", "+00:00")
+            scheduled_dt = datetime.fromisoformat(ts_norm)
+            now_ref = (
+                datetime.now(tz=timezone.utc)
+                if scheduled_dt.tzinfo is not None
+                else datetime.utcnow()
+            )
+            if (now_ref - scheduled_dt).total_seconds() > 300:
+                return False, (
+                    f"schedule_task: 'scheduled_for' is >5 min in the past: {scheduled_for}"
+                )
+        except ValueError as exc:
+            return False, f"schedule_task: invalid 'scheduled_for' timestamp: {exc}"
+
+        # ── 6. Build TaskMetadata ──────────────────────────────────────────
+        task_id = params.get("task_id") or f"argus-{task_type}-{uuid.uuid4().hex[:8]}"
+        metadata = TaskMetadata(
+            task_id=task_id,
+            task_type=task_type,
+            command=command,
+            scheduled_for=scheduled_for,
+            created_at=datetime.utcnow().isoformat(),
+            created_by="argus",
+            status=TaskStatus.PENDING.value,
+            retry_count=0,
+            max_retries=int(params.get("max_retries", 3)),
+            priority=int(params.get("priority", 50)),
+            vram_sensitive=bool(params.get("vram_sensitive", False)),
+        )
+
+        # ── 7. Persist metadata + enqueue ──────────────────────────────────
+        redis_url = os.getenv("TASK_SCHEDULER_REDIS_URL", "redis://localhost:6379")
+        try:
+            client = await _aioredis.from_url(redis_url, decode_responses=True)
+            try:
+                queue = RedisQueueManager(client)
+                await queue.set_task_metadata(task_id, metadata)
+                await queue.schedule_task(task_id, scheduled_for, task_type)
+            finally:
+                await client.aclose()
+        except Exception as exc:
+            log.error("schedule_task: Redis error for task %s: %s", task_id, exc)
+            return False, f"schedule_task: Redis error: {exc}"
+
+        log.info(
+            "schedule_task: enqueued task_id=%s type=%s scheduled_for=%s",
+            task_id, task_type, scheduled_for,
+        )
+        return True, f"task_id={task_id}"
+
     # ── EXECUTION ─────────────────────────────────────────────
 
-    def _fire(self, step: PlanStep, now: datetime, is_catchup: bool = False) -> None:
+    async def _fire(self, step: PlanStep, now: datetime, is_catchup: bool = False) -> None:
         if step.requires_approval:
             self._request_approval(step)
             return
@@ -533,7 +699,7 @@ class ArgusOrchestrator:
             self._send_morning_digest(now)
             return
 
-        ok, detail = self._execute_action(step)
+        ok, detail = await self._execute_action(step)
 
         status_key = (
             "catchup_ok" if ok else "catchup_fail"
@@ -629,7 +795,7 @@ class ArgusOrchestrator:
 
     # ── Catch-up logic ─────────────────────────────────────────────────────────
 
-    def _run_catchups(self, now: datetime) -> None:
+    async def _run_catchups(self, now: datetime) -> None:
         """
         For each scheduled step: if there was an expected cron firing in the last
         CATCH_UP_HOURS that was not covered by last_run → fire as catch-up.
@@ -709,7 +875,7 @@ class ArgusOrchestrator:
                     f"Ожидался: {missed_str}, запускаю сейчас."
                 )
                 self._state.mark_run(step.id, now)
-                self._fire(step, now, is_catchup=True)
+                await self._fire(step, now, is_catchup=True)
                 launched_this_tick += 1
                 if launched_this_tick >= CATCH_UP_MAX_PER_TICK:
                     log.info(
@@ -719,103 +885,6 @@ class ArgusOrchestrator:
                     )
                     break
 
-
-# ── Step execution ─────────────────────────────────────────────────────────
-def _fire(self, step: PlanStep, now: datetime, is_catchup: bool = False) -> None:
-    if step.requires_approval:
-        self._request_approval(step)
-        return
-
-    if step.action == "send_digest":
-        self._send_morning_digest(now)
-        return
-
-    ok, detail = self._execute_action(step)
-
-    status_key = ("catchup_ok" if ok else "catchup_fail") if is_catchup else ("ok" if ok else "failed")
-    icon = "✅" if ok else "❌"
-    catchup_tag = " (catch-up)" if is_catchup else ""
-
-    # 🔧 AUTO-REPAIR
-    if not ok:
-        try:
-            log.warning("triggering repair agent for step: %s", step.id)
-
-            repair_result = self._repair.fix_from_context({
-                "error": detail,
-                "context": {
-                    "step_id": step.id,
-                    "plan": step.plan_name,
-                    "action": step.action,
-                    "params": step.params,
-                },
-                "output": detail,
-            })
-
-            self._on_notify(
-                f"🛠 <b>Auto-repair выполнен</b>\n"
-                f"Step: <code>{step.id}</code>\n"
-                f"<pre>{repair_result[:500]}</pre>"
-            )
-
-        except Exception as repair_error:
-            self._on_notify(
-                f"❌ <b>Repair failed</b>\n"
-                f"Step: <code>{step.id}</code>\n"
-                f"<pre>{str(repair_error)[:300]}</pre>"
-            )
-
-    # запись состояния
-    self._state.mark_run(step.id, now, status=status_key, detail=detail)
-
-    if step.include_in_digest:
-        self._state.push_digest(DigestEntry(
-            ts=now,
-            step_id=step.id,
-            description=step.description,
-            status=status_key,
-            detail=detail[:200],
-            is_catchup=is_catchup,
-        ))
-
-    # уведомления
-    if not ok:
-        self._on_notify(
-            f"{icon} <b>Step failed{catchup_tag}</b>\n"
-            f"Step: <code>{step.id}</code>\n"
-            f"Plan: {step.plan_name}\n"
-            f"Error: <pre>{detail[:300]}</pre>"
-        )
-    elif is_catchup:
-        self._on_notify(
-            f"↩️ {icon} <b>Catch-up выполнен</b>: <code>{step.id}</code>\n"
-            f"{detail[:200]}"
-        )
-
-
-    # ── ACTION EXECUTION ─────────────────────────────────────
-
-    def _execute_action(self, step: PlanStep) -> tuple[bool, str]:
-        action = step.action
-
-        if action == "create_task":
-            return _exec_create_task(step.params)
-
-        elif action == "run_script":
-            return _exec_run_script(step.params)
-
-        elif action == "notify":
-            msg = step.params.get("message", step.description)
-            self._on_notify(msg)
-            return True, "notified"
-
-        elif action == "send_digest":
-            return True, "digest"
-
-        else:
-            log.warning("unknown action: %s", action)
-            return False, f"unknown action: {action}"
-
     # ── APPROVAL FLOW ───────────────────────────────────────
 
     def _request_approval(self, step: PlanStep) -> None:
@@ -840,7 +909,12 @@ def _fire(self, step: PlanStep, now: datetime, is_catchup: bool = False) -> None
         if step is None:
             return "⚠️ Шаг не найден или уже выполнен"
 
-        ok, detail = self._execute_action(step)
+        loop = self._event_loop
+        if loop and loop.is_running():
+            fut = asyncio.run_coroutine_threadsafe(self._execute_action(step), loop)
+            ok, detail = fut.result(timeout=300)
+        else:
+            return "⚠️ Orchestrator loop not running — approve unavailable"
 
         status = "approved+ok" if ok else "approved+fail"
         icon = "✅" if ok else "❌"
@@ -920,7 +994,12 @@ def _fire(self, step: PlanStep, now: datetime, is_catchup: bool = False) -> None
             return f"🔐 <b>{step.description}</b>\nОтправлен запрос на одобрение."
         if step.action == "send_digest":
             return self.send_digest_now()
-        ok, detail = self._execute_action(step)
+        loop = self._event_loop
+        if loop and loop.is_running():
+            fut = asyncio.run_coroutine_threadsafe(self._execute_action(step), loop)
+            ok, detail = fut.result(timeout=300)
+        else:
+            return "⚠️ Orchestrator loop not running — run_step_now unavailable"
         self._state.mark_run(step.id, now, status="ok" if ok else "failed", detail=detail)
         icon = "✅" if ok else "❌"
         return f"{icon} <b>{step.description}</b>\n<pre>{detail[:400]}</pre>"
@@ -1197,38 +1276,6 @@ def _fire(self, step: PlanStep, now: datetime, is_catchup: bool = False) -> None
                 lines.append(f"  • <b>{dt.strftime('%d.%m %H:%M')}</b> — <code>{sid}</code> ({desc[:60]})")
 
         return "\n".join(lines)
-        
-        # 🔧 AUTO-REPAIR PIPELINE ───────────────────────────────────────────────
-
-        if not ok:
-            try:
-                log.warning("triggering repair agent for step: %s", step.id)
-
-                repair_result = self._repair.fix_from_context({
-                    "error": detail,
-                    "context": {
-                        "step_id": step.id,
-                        "plan": step.plan_name,
-                        "action": step.action,
-                        "params": step.params,
-                    },
-                    "output": detail,
-                })
-
-                self._on_notify(
-                    f"🛠 <b>Auto-repair выполнен</b>\n"
-                    f"Step: <code>{step.id}</code>\n"
-                    f"<pre>{repair_result[:500]}</pre>"
-                )
-
-            except Exception as repair_error:
-                self._on_notify(
-                    f"❌ <b>Repair failed</b>\n"
-                    f"Step: <code>{step.id}</code>\n"
-                    f"<pre>{str(repair_error)[:300]}</pre>"
-                )
-                
-        return "\n".join(lines)
 
 
 # ── Utility: restore pending approvals on restart ───────────────────────────
@@ -1298,6 +1345,7 @@ def create_orchestrator(
     on_notify: Callable[[str], None],
     on_approval_request: Callable[[PlanStep], None],
     storage: "ArgusStorage | None" = None,
+    on_missed_start_report: Callable[[list], None] | None = None,
 ) -> ArgusOrchestrator:
     """
     Factory helper (чтобы удобно подключать в argus_bot).
@@ -1306,6 +1354,7 @@ def create_orchestrator(
         on_notify=on_notify,
         on_approval_request=on_approval_request,
         storage=storage,
+        on_missed_start_report=on_missed_start_report,
     )
 
     orch.reload_plans()
@@ -1314,6 +1363,257 @@ def create_orchestrator(
     return orch
 
 
+# ── Scheduler health query (used by argus_bot /scheduler command) ────────────
+
+async def get_scheduler_status() -> dict:
+    """Query Redis for scheduler daemon health and queue counts.
+
+    Returns a dict with keys:
+        available       — scheduler imports succeeded
+        daemon_alive    — heartbeat key present and fresh
+        heartbeat_age_s — seconds since last heartbeat write, or None
+        stale           — heartbeat older than 150s threshold
+        pending         — tasks waiting to run
+        running         — tasks currently executing
+        retrying        — tasks in retry backoff
+        failed          — tasks that exhausted retries
+        held_for_review — tasks held after missed-start scan
+        error           — str, only present on Redis connection failure
+    """
+    if not _SCHEDULER_AVAILABLE:
+        return {
+            "available": False,
+            "daemon_alive": False,
+            "heartbeat_age_s": None,
+            "stale": False,
+            "pending": 0,
+            "running": 0,
+            "retrying": 0,
+            "failed": 0,
+            "held_for_review": 0,
+        }
+
+    redis_url = (
+        os.getenv("AIMS_REDIS_URL")
+        or os.getenv("TASK_SCHEDULER_REDIS_URL")
+        or "redis://localhost:6379"
+    )
+    _HEARTBEAT_STALE_THRESHOLD = 150  # 2.5× the 60s write interval
+
+    try:
+        client = await _aioredis.from_url(redis_url, decode_responses=True)
+        try:
+            raw_hb = await client.get("scheduler:daemon:heartbeat")
+            pending = await client.zcard("scheduler:tasks:pending")
+            running = await client.zcard("scheduler:tasks:running")
+            retrying = await client.zcard("scheduler:tasks:retrying")
+            failed = await client.zcard("scheduler:tasks:failed")
+            held = await client.zcard("scheduler:tasks:missed_startup_review")
+        finally:
+            await client.aclose()
+    except Exception as exc:
+        log.warning("get_scheduler_status: Redis error — %s", exc)
+        return {
+            "available": True,
+            "daemon_alive": False,
+            "heartbeat_age_s": None,
+            "stale": True,
+            "pending": 0,
+            "running": 0,
+            "retrying": 0,
+            "failed": 0,
+            "held_for_review": 0,
+            "error": str(exc),
+        }
+
+    if raw_hb is None:
+        return {
+            "available": True,
+            "daemon_alive": False,
+            "heartbeat_age_s": None,
+            "stale": True,
+            "pending": pending,
+            "running": running,
+            "retrying": retrying,
+            "failed": failed,
+            "held_for_review": held,
+        }
+
+    try:
+        age = int(time.time()) - int(raw_hb)
+        stale = age > _HEARTBEAT_STALE_THRESHOLD
+    except ValueError:
+        age = None
+        stale = True
+
+    return {
+        "available": True,
+        "daemon_alive": age is not None and not stale,
+        "heartbeat_age_s": age,
+        "stale": stale,
+        "pending": pending,
+        "running": running,
+        "retrying": retrying,
+        "failed": failed,
+        "held_for_review": held,
+    }
+
+
+async def get_scheduler_detail() -> dict:
+    """Extended scheduler status: base health + last completed/failed tasks + totals.
+
+    Calls get_scheduler_status() (stable contract, 51 tests) then opens a second
+    Redis connection to fetch completed-queue stats and last-task metadata.
+
+    Additional keys beyond get_scheduler_status():
+        completed_total — int, count of tasks ever completed (zcard)
+        last_completed  — dict with task_id/task_type/created_at/display_name, or None
+        last_failed     — dict with task_id/task_type/error/created_at, or None
+    """
+    base = await get_scheduler_status()
+
+    # Seed defaults so callers always get the keys regardless of path
+    base.setdefault("completed_total", 0)
+    base.setdefault("last_completed", None)
+    base.setdefault("last_failed", None)
+
+    if not base.get("available") or "error" in base:
+        return base
+
+    redis_url = (
+        os.getenv("AIMS_REDIS_URL")
+        or os.getenv("TASK_SCHEDULER_REDIS_URL")
+        or "redis://localhost:6379"
+    )
+    try:
+        client = await _aioredis.from_url(redis_url, decode_responses=True)
+        try:
+            completed_total = await client.zcard("scheduler:tasks:completed")
+            last_cmp_ids = await client.zrange("scheduler:tasks:completed", -1, -1)
+            last_fail_ids = await client.zrange("scheduler:tasks:failed", -1, -1)
+
+            last_completed = None
+            if last_cmp_ids:
+                raw = await client.hgetall(f"scheduler:task:{last_cmp_ids[0]}")
+                if raw:
+                    last_completed = {
+                        "task_id": raw.get("task_id", last_cmp_ids[0]),
+                        "task_type": raw.get("task_type", "unknown"),
+                        "display_name": raw.get("display_name") or raw.get("task_type", ""),
+                        "created_at": raw.get("created_at", ""),
+                    }
+
+            last_failed = None
+            if last_fail_ids:
+                raw = await client.hgetall(f"scheduler:task:{last_fail_ids[0]}")
+                if raw:
+                    last_failed = {
+                        "task_id": raw.get("task_id", last_fail_ids[0]),
+                        "task_type": raw.get("task_type", "unknown"),
+                        "error": raw.get("error", "")[:120],
+                        "created_at": raw.get("created_at", ""),
+                    }
+        finally:
+            await client.aclose()
+    except Exception as exc:
+        log.warning("get_scheduler_detail: Redis error during detail query — %s", exc)
+        # Return base status intact; detail fields remain at defaults
+        return base
+
+    base["completed_total"] = completed_total
+    base["last_completed"] = last_completed
+    base["last_failed"] = last_failed
+    return base
+
+
+async def get_scheduler_backpressure_detail() -> dict:
+    """Extends get_scheduler_detail() with real-time backpressure and slot occupancy.
+
+    Calls get_scheduler_detail() (which calls get_scheduler_status()) then opens
+    a third Redis connection to read running task metadata and derive slot states,
+    mutex lock state, active resource key locks, and queue pressure level.
+
+    Additional keys beyond get_scheduler_detail():
+        slot_states          — {slot14, slot32, slot120} → "free" | "busy"
+        mutex_locked         — bool; True if slot32 or slot120 is running (mutex active)
+        running_slots        — list[{task_id, task_type, model_slot}] for slotted tasks
+        active_resource_keys — list[str] of resource_key values currently held
+        global_limit         — int; ResourcePolicyConfig default (3)
+        queue_pressure       — "OK" | "WARN" | "CRITICAL"
+    """
+    from ops.scheduler.resource_policy import ResourcePolicyConfig
+
+    base = await get_scheduler_detail()
+
+    # Seed backpressure defaults so callers always get all keys
+    base.setdefault("slot_states", {"slot14": "free", "slot32": "free", "slot120": "free"})
+    base.setdefault("mutex_locked", False)
+    base.setdefault("running_slots", [])
+    base.setdefault("active_resource_keys", [])
+    base.setdefault("global_limit", ResourcePolicyConfig().global_max_concurrent)
+    base.setdefault("queue_pressure", "OK")
+
+    if not base.get("available") or "error" in base:
+        return base
+
+    redis_url = (
+        os.getenv("AIMS_REDIS_URL")
+        or os.getenv("TASK_SCHEDULER_REDIS_URL")
+        or "redis://localhost:6379"
+    )
+    try:
+        client = await _aioredis.from_url(redis_url, decode_responses=True)
+        try:
+            running_ids = await client.zrange("scheduler:tasks:running", 0, -1)
+            running_slots: list[dict] = []
+            active_resource_keys: list[str] = []
+            for task_id in running_ids:
+                raw = await client.hgetall(f"scheduler:task:{task_id}")
+                if not raw:
+                    continue
+                slot = raw.get("model_slot") or None
+                rkey = raw.get("resource_key") or None
+                if slot:
+                    running_slots.append({
+                        "task_id": task_id,
+                        "task_type": raw.get("task_type", "unknown"),
+                        "model_slot": slot,
+                    })
+                if rkey:
+                    active_resource_keys.append(rkey)
+        finally:
+            await client.aclose()
+    except Exception as exc:
+        log.warning("get_scheduler_backpressure_detail: Redis error — %s", exc)
+        return base
+
+    occupied_slots = {r["model_slot"] for r in running_slots}
+    slot_states = {
+        "slot14":  "busy" if "slot14"  in occupied_slots else "free",
+        "slot32":  "busy" if "slot32"  in occupied_slots else "free",
+        "slot120": "busy" if "slot120" in occupied_slots else "free",
+    }
+    mutex_locked = slot_states["slot32"] == "busy" or slot_states["slot120"] == "busy"
+
+    global_limit = ResourcePolicyConfig().global_max_concurrent
+    running_count = base.get("running", 0)
+    pending_count = base.get("pending", 0)
+    if running_count >= global_limit:
+        queue_pressure = "CRITICAL"
+    elif running_count >= global_limit - 1 or pending_count > 5:
+        queue_pressure = "WARN"
+    else:
+        queue_pressure = "OK"
+
+    base["slot_states"] = slot_states
+    base["mutex_locked"] = mutex_locked
+    base["running_slots"] = running_slots
+    base["active_resource_keys"] = active_resource_keys
+    base["global_limit"] = global_limit
+    base["queue_pressure"] = queue_pressure
+    return base
+
+
 def _argus_problem_name(self, step_id: str, description: str) -> str:
     return normalize_problem_name(step_id, description)
 
diff --git a/ops/axi_bot.py b/ops/axi_bot.py
index 8fafeea..776cb46 100644
--- a/ops/axi_bot.py
+++ b/ops/axi_bot.py
@@ -2347,6 +2347,17 @@ def _strip_axi_prefix(text: str) -> str:
     return clean
 
 
+_AXI_STOP_TOKENS: frozenset[str] = frozenset({
+    "stop", "стоп", "cancel", "отмена", "halt", "прекрати", "стоп репорт",
+})
+
+
+def _is_axi_stop_command(text: str) -> bool:
+    """Return True if the (already prefix-stripped) text is a stop/cancel command."""
+    low = text.strip().lower()
+    return low in _AXI_STOP_TOKENS or any(low.startswith(t + " ") for t in _AXI_STOP_TOKENS)
+
+
 # ── Command handlers ───────────────────────────────────────────────────────────
 
 async def cmd_start(update: Update, ctx: ContextTypes.DEFAULT_TYPE) -> None:
@@ -4050,6 +4061,17 @@ async def _process_request_text(
         text = text.replace(f"@{bot_username}", "").strip()
     text = _strip_axi_prefix(text)
 
+    # Stop/cancel: clear all pending state and acknowledge — do this before any
+    # routing so stale pending-state dicts can't steal the message.
+    if _is_axi_stop_command(text):
+        if _chat_id is not None:
+            _PENDING_AI_LABEL_REMOVAL.pop(_chat_id, None)
+            _pending_intent.pop(_chat_id, None)
+            _PENDING_DOC_INTAKE.pop(_chat_id, None)
+            _PENDING_VIDEO.pop(_chat_id, None)
+        await update.message.reply_text("Stopped.")
+        return
+
     if await _is_omi_owned_intent(text):
         log.info("Axi delegating DOCSREG intent to Omi: chat_id=%s", _chat_id)
         return
diff --git a/ops/cyclic_doc_generation_pipeline.py b/ops/cyclic_doc_generation_pipeline.py
index aeec8ca..ca1add2 100644
--- a/ops/cyclic_doc_generation_pipeline.py
+++ b/ops/cyclic_doc_generation_pipeline.py
@@ -81,10 +81,6 @@ ROOT = _resolve_workspace_root()
 STATUS_DIR = ROOT / "aims_workspace" / "agent_architecture_status"
 STATUS_DIR.mkdir(parents=True, exist_ok=True)
 
-# Max sections per Bedrock audit call for chunked path.
-# 14 sections × ~350 tokens/finding ≈ 4900 output tokens — well within 8000 limit.
-_AUDIT_CHUNK_SIZE = 14
-
 # Skills
 from ops.cyclic_skills import (  # noqa: E402
     sections_semantically_match,
@@ -119,6 +115,10 @@ from ops.agents.skills.docsreg_phase1_convergence import (  # noqa: E402
 from ops.agents.skills.docsreg_phase2_nesting import apply_phase2_nesting  # noqa: E402
 from ops.agents.skills.docsreg_phase3_content_quality import select_phase3_recommendations  # noqa: E402
 from ops.docgen.render_visual_qa import render_docx_for_qa, RenderMetrics  # noqa: E402
+from ops.docgen.audits.audit_budget_planner import (  # noqa: E402
+    AuditUnit,
+    plan_audit_calls,
+)
 from ops.docgen.universal_overlay.enforcement_contracts import (  # noqa: E402
     EnforcementContext,
     GateResult,
@@ -2715,20 +2715,73 @@ def _claude_code_audit(
     }
     section_names = list(doc_sections)
     findings_already_grounded = False
+    planning_units = []
+    for index, name in enumerate(section_names, start=1):
+        reference_excerpt = (
+            match_reference_section(reference_text, name)
+            if reference_text
+            else ""
+        )
+        planning_units.append(
+            AuditUnit(
+                unit_id=f"section-{index:03d}",
+                title=name,
+                content=(
+                    doc_sections[name]
+                    + (
+                        "\n\n[REFERENCE EXCERPT]\n" + reference_excerpt
+                        if reference_excerpt
+                        else ""
+                    )
+                ),
+            )
+        )
+    audit_budget = plan_audit_calls(
+        planning_units,
+        global_context="\n".join(
+            (
+                topic,
+                omi_standards_str,
+                axi_standards_str,
+                axi_recs_str,
+                struct_info,
+            )
+        ),
+        include_final_deliverable_call=False,
+        stage="cyclic_integrated_semantic_construction",
+    )
+    if evidence_dir:
+        evidence_dir.mkdir(parents=True, exist_ok=True)
+        (evidence_dir / "audit_token_budget.json").write_text(
+            json.dumps(audit_budget, indent=2, ensure_ascii=False),
+            encoding="utf-8",
+        )
+    if audit_budget["status"] != "PASS":
+        log.error(
+            "[CLAUDE-AUDIT] Token/call budget blocked audit: %s",
+            audit_budget.get("blocker"),
+        )
+        return None
 
     # ── Chunked audit path (large documents) ───────────────────────────────
-    # When section count exceeds _AUDIT_CHUNK_SIZE (14), split into batches
-    # of ≤14 sections, call _audit_section_batch() per batch, then run a
-    # lightweight synthesis call for document-level quality metrics.
-    if len(section_names) > _AUDIT_CHUNK_SIZE:
-        n_batches = -(-len(section_names) // _AUDIT_CHUNK_SIZE)  # ceil division
+    # Split only when the estimated packet exceeds the auditor's quality-
+    # oriented attention budget. Chunk boundaries come from semantic sections,
+    # not an arbitrary fixed number of headings.
+    if audit_budget["semantic_audit_calls"] > 1:
+        planned_batches = [
+            list(chunk["unit_titles"])
+            for chunk in audit_budget["chunks"]
+        ]
+        n_batches = len(planned_batches)
         log.info(
-            "[CLAUDE-AUDIT] Chunked audit: %d sections → %d batches of ≤%d",
-            len(section_names), n_batches, _AUDIT_CHUNK_SIZE,
+            "[CLAUDE-AUDIT] Adaptive chunked audit: %d sections → %d batches; "
+            "estimated input=%d tokens",
+            len(section_names),
+            n_batches,
+            audit_budget["estimated_total_input_tokens"],
         )
         all_findings: list[dict] = []
-        for i in range(n_batches):
-            batch = section_names[i * _AUDIT_CHUNK_SIZE : (i + 1) * _AUDIT_CHUNK_SIZE]
+        for i, batch in enumerate(planned_batches):
             all_findings.extend(
                 _audit_section_batch(
                     batch_idx=i,
@@ -2812,12 +2865,15 @@ def _claude_code_audit(
         log.info("[CLAUDE-AUDIT] Chunked audit: %d findings merged, synthesis %s",
                  len(all_findings), "OK" if synth_parsed else "DEFAULT")
 
-    # ── Single-pass audit path (small/medium documents ≤ _AUDIT_CHUNK_SIZE) ─
+    # ── Single-pass audit path within the soft attention-token budget ──────
     else:
         log.info(
             "[CLAUDE-AUDIT] Single-pass full audit: %d sections, "
-            "%d document chars, %d reference chars",
-            len(section_names), len(doc_excerpt), len(reference_text),
+            "%d document chars, %d reference chars, %d estimated tokens",
+            len(section_names),
+            len(doc_excerpt),
+            len(reference_text),
+            audit_budget["estimated_total_input_tokens"],
         )
         audit_prompt = f"""You are the independent teacher/judge for an Asset Integrity Management document.
 
diff --git a/ops/docsreg/skills/testing/reports/evidence_package.json b/ops/docsreg/skills/testing/reports/evidence_package.json
index 700f2eb..b0b7767 100644
--- a/ops/docsreg/skills/testing/reports/evidence_package.json
+++ b/ops/docsreg/skills/testing/reports/evidence_package.json
@@ -1,8 +1,8 @@
 {
   "metadata": {
     "framework_version": "PHASE_7_v1",
-    "generated_at": "2026-06-17T08:50:58.516687",
-    "execution_timestamp": "2026-06-17T08:50:58.516632",
+    "generated_at": "2026-06-17T12:42:42.932640",
+    "execution_timestamp": "2026-06-17T12:42:42.932604",
     "total_skills": 6,
     "total_cases": 30,
     "overall_verdict": "BLOCKED"
@@ -35,7 +35,7 @@
       "cases_promoted": 3,
       "cases_advisory": 1,
       "cases_blocked": 1,
-      "executed_at": "2026-06-17T08:50:58.516632",
+      "executed_at": "2026-06-17T12:42:42.932604",
       "cases": [
         {
           "test_case_id": "timeout_001",
@@ -137,7 +137,7 @@
       "cases_promoted": 3,
       "cases_advisory": 1,
       "cases_blocked": 1,
-      "executed_at": "2026-06-17T08:50:58.516632",
+      "executed_at": "2026-06-17T12:42:42.932604",
       "cases": [
         {
           "test_case_id": "structure_001",
@@ -237,7 +237,7 @@
       "cases_promoted": 3,
       "cases_advisory": 1,
       "cases_blocked": 1,
-      "executed_at": "2026-06-17T08:50:58.516632",
+      "executed_at": "2026-06-17T12:42:42.932604",
       "cases": [
         {
           "test_case_id": "policy_001",
@@ -339,7 +339,7 @@
       "cases_promoted": 3,
       "cases_advisory": 1,
       "cases_blocked": 1,
-      "executed_at": "2026-06-17T08:50:58.516632",
+      "executed_at": "2026-06-17T12:42:42.932604",
       "cases": [
         {
           "test_case_id": "quality_001",
@@ -442,7 +442,7 @@
       "cases_promoted": 3,
       "cases_advisory": 1,
       "cases_blocked": 1,
-      "executed_at": "2026-06-17T08:50:58.516632",
+      "executed_at": "2026-06-17T12:42:42.932604",
       "cases": [
         {
           "test_case_id": "dataloss_001",
@@ -544,7 +544,7 @@
       "cases_promoted": 3,
       "cases_advisory": 1,
       "cases_blocked": 1,
-      "executed_at": "2026-06-17T08:50:58.516632",
+      "executed_at": "2026-06-17T12:42:42.932604",
       "cases": [
         {
           "test_case_id": "resource_001",
diff --git a/ops/docsreg/skills/testing/reports/evidence_report.txt b/ops/docsreg/skills/testing/reports/evidence_report.txt
index cc3d274..e41884d 100644
--- a/ops/docsreg/skills/testing/reports/evidence_report.txt
+++ b/ops/docsreg/skills/testing/reports/evidence_report.txt
@@ -5,8 +5,8 @@ PHASE 7 SKILL TESTING FRAMEWORK — EVIDENCE REPORT
 METADATA
 ----------------------------------------------------------------------------------------------------
 framework_version              PHASE_7_v1
-generated_at                   2026-06-17T08:50:58.516687
-execution_timestamp            2026-06-17T08:50:58.516632
+generated_at                   2026-06-17T12:42:42.932640
+execution_timestamp            2026-06-17T12:42:42.932604
 total_skills                   6
 total_cases                    30
 overall_verdict                BLOCKED
diff --git a/ops/logi/model_data_factory.py b/ops/logi/model_data_factory.py
index f0275cc..4790bce 100644
--- a/ops/logi/model_data_factory.py
+++ b/ops/logi/model_data_factory.py
@@ -24,13 +24,13 @@ AGENT_SKILL_REGISTRY = ROOT / "ops/agents/agent_skill_registry.yaml"
 
 
 DISCOVERY_SOURCES = [
-    "docs/AIMS_GSTACK_INTEGRATION_STATUS.md",
+    "docs/AIMS_FULLSTACK_INTEGRATION_STATUS.md",
     "docs/AUTONOMY_CONTROL_PLANE_V1_CERTIFICATION.md",
     "docs/LOGI_PROCESS_INTEGRITY_ORCHESTRATOR_V1_CERTIFICATION.md",
     "docs/AUTONOMY_NEXT_STAGE_LONG_RUNNING_SELF_REGULATION.md",
     "ops/autonomy_control/README.md",
     "ops/agents/agent_skill_registry.yaml",
-    "docs/agents/AIMS_GSTACK_AGENT_MAPPING.md",
+    "docs/agents/AIMS_FULLSTACK_AGENT_MAPPING.md",
     "docs/agents/skills",
     "ops/logi",
     "ops/evals",
@@ -227,7 +227,7 @@ def build_slot_inventory(topics: list[Topic]) -> list[dict[str, Any]]:
                 "Russian/English mixed workflow dialogue",
             ],
             "source_files": [
-                "docs/AIMS_GSTACK_INTEGRATION_STATUS.md",
+                "docs/AIMS_FULLSTACK_INTEGRATION_STATUS.md",
                 "ops/logi/model_level_assessor.py",
                 "aims_workspace/model_tuning_readiness/certifications/model_tuning_readiness_chain_v1_20260514_145843/model_level_inventory.json",
             ],
@@ -251,7 +251,7 @@ def build_slot_inventory(topics: list[Topic]) -> list[dict[str, Any]]:
                 "resource policy and slot32/slot120 conflict decisions",
             ],
             "source_files": [
-                "docs/AIMS_GSTACK_INTEGRATION_STATUS.md",
+                "docs/AIMS_FULLSTACK_INTEGRATION_STATUS.md",
                 "ops/logi/model_level_assessor.py",
                 "ops/logi/model_tuning_readiness_chain.py",
             ],
@@ -270,7 +270,7 @@ def build_slot_inventory(topics: list[Topic]) -> list[dict[str, Any]]:
                 "document structure/completeness and client-ready quality supervision",
                 "independent heavy audit when escalation criteria are met",
             ],
-            "source_files": ["docs/AIMS_GSTACK_INTEGRATION_STATUS.md", "ops/logi/model_level_assessor.py"],
+            "source_files": ["docs/AIMS_FULLSTACK_INTEGRATION_STATUS.md", "ops/logi/model_level_assessor.py"],
             "evidence_paths": ["ops/evals/dgx_model_concurrency_policy_smoke.py"],
             "tuning_topics_required": ["optional audit/eval rubric material only"],
             "missing_from_initial_prompt": [],
diff --git a/ops/logi/model_data_snapshot.py b/ops/logi/model_data_snapshot.py
index 9ab3e2c..f69e1e1 100644
--- a/ops/logi/model_data_snapshot.py
+++ b/ops/logi/model_data_snapshot.py
@@ -42,7 +42,7 @@ def build_dataset_snapshot(
         "snapshot_id": f"model_data_snapshot_{certification_id}",
         "created_at": datetime.now(timezone.utc).isoformat(),
         "source_files": [
-            "docs/AIMS_GSTACK_INTEGRATION_STATUS.md",
+            "docs/AIMS_FULLSTACK_INTEGRATION_STATUS.md",
             "ops/agents/agent_skill_registry.yaml",
             "aims_workspace/model_tuning_readiness/certifications/model_tuning_readiness_chain_v1_20260514_145843",
         ],
diff --git a/ops/logi/planned_actions.py b/ops/logi/planned_actions.py
index 51cb3b2..80149d8 100644
--- a/ops/logi/planned_actions.py
+++ b/ops/logi/planned_actions.py
@@ -176,13 +176,13 @@ def build_sample_planned_action(goal_id: str) -> PlannedAction:
         due_time="",
         agent_chain=["argus/watchdog", "knomi", "logi"],
         acceptance_criteria=[
-            "All required agents reachable (gstack audit PASS)",
+            "All required agents reachable (fullstack audit PASS)",
             "DGX model concurrency policy PASS — no slot32/slot120 conflict",
             "Post-corrective stability audit OK or WARN historical-only",
             "Final report produced and stored",
         ],
         validation_plan=(
-            "1. Re-run gstack_agent_integration_audit\n"
+            "1. Re-run fullstack_agent_integration_audit\n"
             "2. Re-run dgx_model_concurrency_policy_smoke\n"
             "3. Re-run post_restart_service_stability_audit\n"
             "4. Verify ledger continuity"
diff --git a/ops/logi/process_goals.py b/ops/logi/process_goals.py
index 124426b..ff98a95 100644
--- a/ops/logi/process_goals.py
+++ b/ops/logi/process_goals.py
@@ -144,7 +144,7 @@ def build_default_integrity_goal(requested_by: str = "logi_certification") -> Pr
             "control_plane",
         ],
         checks=[
-            "gstack_agent_integration_audit",
+            "fullstack_agent_integration_audit",
             "post_restart_service_stability_audit",
             "dgx_model_concurrency_policy_smoke",
             "agent_status_all",
@@ -171,8 +171,8 @@ def build_default_integrity_goal(requested_by: str = "logi_certification") -> Pr
             blocking=True,
         ),
         SuccessCriterion(
-            description="Gstack integration audit PASS",
-            check_fn="gstack_agent_integration_audit",
+            description="Fullstack integration audit PASS",
+            check_fn="fullstack_agent_integration_audit",
             required=True,
             owner_agent_or_tool="argus",
             expected_evidence="All 14 agents integrated",
diff --git a/ops/logi/process_integrity_orchestrator.py b/ops/logi/process_integrity_orchestrator.py
index 0c9a062..2552cd6 100644
--- a/ops/logi/process_integrity_orchestrator.py
+++ b/ops/logi/process_integrity_orchestrator.py
@@ -31,7 +31,7 @@ RUNS_ROOT = ROOT / "aims_workspace/logi_process_integrity/runs"
 CERTS_ROOT = ROOT / "aims_workspace/logi_process_integrity/certifications"
 
 _AUDIT_SCRIPTS = {
-    "gstack_agent_integration_audit": ROOT / "ops/evals/gstack_agent_integration_audit.py",
+    "fullstack_agent_integration_audit": ROOT / "ops/evals/fullstack_agent_integration_audit.py",
     "post_restart_service_stability_audit": ROOT / "ops/evals/post_restart_service_stability_audit.py",
     "dgx_model_concurrency_policy_smoke": ROOT / "ops/evals/dgx_model_concurrency_policy_smoke.py",
 }
@@ -550,7 +550,7 @@ def _create_corrective_plan(goal: ProcessGoal, ledger: ProcessLedger) -> list[Co
 
         # Map criterion to responsible agent
         agent_map = {
-            "gstack_agent_integration_audit": "argus",
+            "fullstack_agent_integration_audit": "argus",
             "control_plane_smoke": "logi",
             "dgx_model_concurrency_policy_smoke": "argus",
             "post_restart_service_stability_audit": "argus",
diff --git a/ops/logi/process_plan.py b/ops/logi/process_plan.py
index b78ddee..5092241 100644
--- a/ops/logi/process_plan.py
+++ b/ops/logi/process_plan.py
@@ -116,10 +116,10 @@ def build_default_integrity_plan(goal_id: str) -> ProcessPlan:
             required=True,
         ),
         PlanStep(
-            title="Gstack integration audit — 14-agent integration check",
+            title="Fullstack integration audit — 14-agent integration check",
             agent="argus",
-            tool="gstack_agent_integration_audit",
-            check_fn="gstack_agent_integration_audit",
+            tool="fullstack_agent_integration_audit",
+            check_fn="fullstack_agent_integration_audit",
             owner_agent="argus",
             required=True,
         ),
diff --git a/ops/logi/self_test_catalog.py b/ops/logi/self_test_catalog.py
index 8f461c5..9bde868 100644
--- a/ops/logi/self_test_catalog.py
+++ b/ops/logi/self_test_catalog.py
@@ -108,16 +108,16 @@ def build_self_test_catalog() -> list[dict[str, Any]]:
             "can_run_in_72h": True,
         },
         {
-            "test_id": "T006_gstack_integration_audit",
-            "name": "Gstack agent integration audit",
+            "test_id": "T006_fullstack_integration_audit",
+            "name": "Fullstack agent integration audit",
             "owner_agent": "argus",
-            "command_or_callable": "PYTHONPATH=ops python3 ops/evals/gstack_agent_integration_audit.py --run-id <id>",
+            "command_or_callable": "PYTHONPATH=ops python3 ops/evals/fullstack_agent_integration_audit.py --run-id <id>",
             "expected_duration": "1-3m",
             "safe_for_daytime": True,
             "preferred_window": "any",
             "resource_policy": "Read-only integration checks.",
             "required_env_vars": ["PYTHONPATH"],
-            "expected_artifacts": ["aims_workspace/evals/gstack_agent_integration_audit/*"],
+            "expected_artifacts": ["aims_workspace/evals/fullstack_agent_integration_audit/*"],
             "pass_criteria": "14/14 integrated, active stale refs=0.",
             "fail_criteria": "Missing integration or stale active refs.",
             "corrective_owner": "argus",
diff --git a/ops/logi/self_test_planner.py b/ops/logi/self_test_planner.py
index e049126..671d0bf 100644
--- a/ops/logi/self_test_planner.py
+++ b/ops/logi/self_test_planner.py
@@ -81,7 +81,7 @@ def generate_self_test_plan(catalog: list[dict[str, Any]], strategic_goal: str,
         "dependencies": [
             "logi process integrity orchestrator",
             "autonomy control plane smoke path",
-            "argus stability/gstack/dgx audits",
+            "argus stability/fullstack/dgx audits",
             "planned action runner",
         ],
         "resource_constraints": [
diff --git a/ops/logi/traini_tuning_lifecycle.py b/ops/logi/traini_tuning_lifecycle.py
index 7e3a4ba..959d164 100644
--- a/ops/logi/traini_tuning_lifecycle.py
+++ b/ops/logi/traini_tuning_lifecycle.py
@@ -44,7 +44,7 @@ def create_learning_need(
     affected_capability: str,
     current_baseline_score: float | None = None,
     target_score: float | None = None,
-    evaluation_suite: str = "ops/ft/scripts/eval_actions.py + dgx/gstack/stability audits",
+    evaluation_suite: str = "ops/ft/scripts/eval_actions.py + dgx/fullstack/stability audits",
     evidence: dict[str, Any] | None = None,
     priority: str = "medium",
     status: str = "OPEN",
@@ -180,7 +180,7 @@ def planned_improvement_roadmap(
                 "slot": slot,
                 "current_baseline_score": baseline,
                 "target_score": target_score,
-                "required_eval_suite": "traini_runtime_skill_smoke + eval_actions + dgx/stability/gstack audits",
+                "required_eval_suite": "traini_runtime_skill_smoke + eval_actions + dgx/stability/fullstack audits",
                 "data_sufficiency_status": suff,
                 "resource_policy_status": resource_policy.get("status", "FAIL"),
                 "next_safe_action": next_action,
diff --git a/ops/telegram/logi_bot.py b/ops/telegram/logi_bot.py
index 74fbc57..9e24c16 100644
--- a/ops/telegram/logi_bot.py
+++ b/ops/telegram/logi_bot.py
@@ -25,9 +25,46 @@ if _ops not in sys.path:
 
 from logi.conversational_orchestrator import LogiAgent
 from logi.plans import list_plans, load_plan
+from logi.project_goal_intake import ingest_telegram_goal_text
+from logi.project_goal_intake import DEFAULT_ARCHITECTURE_ROOT, GOAL_INDEX_JSON
+from logi.project_goal_autorunner import AutorunnerConfig, status as autorunner_status
 from logi.syntax_interpreter import interpret_syntax_intent
 from logi.syntax_policy import evaluate_intent_policy, render_policy_response
 
+# ── Learning Loop Consumer (EventBus → Phase 2B learning) ─────────────────────
+_LEARNING_LOOP_ENABLED = os.environ.get("AIMS_LEARNING_LOOP_ENABLED", "false").lower() in ("true", "1", "yes")
+
+
+def _start_learning_loop_background():
+    """Start LearningLoopConsumer in a background daemon thread."""
+    import asyncio
+
+    async def _run_consumer():
+        try:
+            from logi.learning_loop_consumer import create_learning_loop_consumer
+            consumer = await create_learning_loop_consumer()
+            if consumer:
+                log.info("LearningLoopConsumer started — subscribed to EventBus")
+                # Keep alive until process exits
+                while not _stop_event.is_set():
+                    await asyncio.sleep(5)
+            else:
+                log.warning("LearningLoopConsumer creation returned None — EventBus may be down")
+        except Exception as exc:
+            log.error("LearningLoopConsumer failed: %s", exc)
+
+    def _thread_target():
+        loop = asyncio.new_event_loop()
+        asyncio.set_event_loop(loop)
+        try:
+            loop.run_until_complete(_run_consumer())
+        finally:
+            loop.close()
+
+    t = threading.Thread(target=_thread_target, name="learning-loop", daemon=True)
+    t.start()
+    log.info("Learning loop background thread started")
+
 # Setup logging
 log = logging.getLogger(__name__)
 
@@ -56,6 +93,12 @@ TOKEN = (
 ).strip()
 
 ALLOWED_USERS_RAW = os.environ.get("LOGI_ALLOWED_USERS", "").strip()
+PROJECT_GOALS_ENABLED = os.environ.get("LOGI_TELEGRAM_PROJECT_GOALS_ENABLED", "true").lower() in (
+    "true",
+    "1",
+    "yes",
+)
+PROJECT_GOAL_INTAKE_MODE = os.environ.get("LOGI_TELEGRAM_GOAL_INTAKE_MODE", "private_text").strip().lower()
 
 if not TOKEN:
     raise SystemExit("Missing LOGI_BOT_TOKEN")
@@ -134,8 +177,51 @@ def _handle_run(chat_id: int, user_id: int, text: str) -> None:
         lock.release()
 
 
+def _handle_project_goal_intake(chat_id: int, user_id: int, msg: dict, text: str) -> None:
+    try:
+        record = ingest_telegram_goal_text(
+            text=text,
+            chat_id=chat_id,
+            user_id=user_id,
+            message_id=int(msg.get("message_id", 0) or 0),
+            chat_type=str(msg.get("chat", {}).get("type") or "private"),
+        )
+        send(
+            chat_id,
+            "Project goal registered for Logi automation.\n"
+            f"Goal ID: {record.goal_id}\n"
+            f"Status: {record.status}\n"
+            f"Architecture: {record.architecture_dir}\n"
+            f"Queued: {record.autorunner_request_path}\n\n"
+            "The Telegram message is now a project architecture artifact; the queue item is transport only.",
+        )
+    except Exception:
+        send(chat_id, "Project goal intake error:\n" + traceback.format_exc()[-2000:])
+
+
+def _project_goal_index_text(limit: int = 10) -> str:
+    index_path = DEFAULT_ARCHITECTURE_ROOT / GOAL_INDEX_JSON
+    if not index_path.exists():
+        return "No project goals registered yet."
+    try:
+        index = json.loads(index_path.read_text(encoding="utf-8"))
+    except Exception:
+        return "Project goal index exists but could not be read."
+    goals = index.get("goals") or []
+    if not goals:
+        return "No project goals registered yet."
+    lines = ["Project goals:"]
+    for item in goals[:limit]:
+        lines.append(
+            f"- {item.get('goal_id')} [{item.get('status')}] "
+            f"{item.get('title')} — {item.get('architecture_dir')}"
+        )
+    return "\n".join(lines)
+
+
 def handle_message(msg: dict) -> None:
     chat_id = msg["chat"]["id"]
+    chat_type = str(msg.get("chat", {}).get("type") or "")
     user_id = int(msg.get("from", {}).get("id", 0))
     text = (msg.get("text") or "").strip()
 
@@ -150,19 +236,21 @@ def handle_message(msg: dict) -> None:
     if text.startswith("/start") or text.startswith("/help"):
         send(chat_id, (
             "LogiAgent — AIMS orchestrator\n\n"
-            "Just type your request in plain language.\n\n"
+            "In this private chat, plain text is registered as a project goal and converted into "
+            "project architecture, task, plan, and implementation artifacts.\n\n"
             "Commands:\n"
+            "/goal <text> — register a project goal from this private chat\n"
+            "/goals — list registered project goals\n"
             "/plans — list your plans\n"
             "/plan <id> — show plan details\n"
             "/clear — reset conversation history\n"
-            "/status — proxy + agent status\n"
+            "/status — proxy, agent, and project automation status\n"
             "/whoami — show your user id\n\n"
             "Examples:\n"
-            "• Создай план для разработки процедуры по HSE для бурения\n"
-            "• Найди документы по ISO 45001\n"
-            "• Что у нас есть по ПЛА?\n"
-            "• Добавь шаг в план: поиск нормативных требований\n"
-            "• Выполни план"
+            "• Build the Logi project automation executor for Telegram goals\n"
+            "• Register the DOCGEN quality loop as a project automation objective\n"
+            "• Create an implementation plan for AIMS maintenance strategy generation\n"
+            "• Verify current project automation readiness"
         ))
         return
 
@@ -175,7 +263,21 @@ def handle_message(msg: dict) -> None:
         health = _proxy_health()
         plans = list_plans(user_id)
         active = [p for p in plans if p.status in ("draft", "active")]
-        send(chat_id, f"Proxy health:\n{health}\n\nActive plans: {len(active)}")
+        auto = autorunner_status(AutorunnerConfig())
+        send(
+            chat_id,
+            f"Proxy health:\n{health}\n\n"
+            f"Active plans: {len(active)}\n\n"
+            "Project automation:\n"
+            f"- status: {auto.get('status')}\n"
+            f"- pid_alive: {auto.get('pid_alive')}\n"
+            f"- pending goals: {auto.get('pending_count')}\n"
+            f"- inbox: {auto.get('inbox_dir')}",
+        )
+        return
+
+    if text.startswith("/goals"):
+        send(chat_id, _project_goal_index_text())
         return
 
     if text.startswith("/plans"):
@@ -189,6 +291,17 @@ def handle_message(msg: dict) -> None:
         send(chat_id, "\n".join(lines))
         return
 
+    if text.startswith("/goal "):
+        goal_text = text.split(" ", 1)[1].strip()
+        if not goal_text:
+            send(chat_id, "Usage: /goal <project goal text>")
+            return
+        if chat_type != "private":
+            send(chat_id, "Project goals are accepted only from the personal Logi chat.")
+            return
+        _handle_project_goal_intake(chat_id, user_id, msg, goal_text)
+        return
+
     # Natural-language-first intent routing for planning/status UX.
     intent = evaluate_intent_policy(interpret_syntax_intent(text))
     if intent.intent_type != "unknown":
@@ -222,6 +335,10 @@ def handle_message(msg: dict) -> None:
     if not text:
         return
 
+    if PROJECT_GOALS_ENABLED and PROJECT_GOAL_INTAKE_MODE == "private_text" and chat_type == "private":
+        _handle_project_goal_intake(chat_id, user_id, msg, text)
+        return
+
     threading.Thread(
         target=_handle_run,
         args=(chat_id, user_id, text),
@@ -245,6 +362,12 @@ def main() -> None:
     print(f"LogiAgent Telegram Bot started")
     print(f"Allowed users: {sorted(ALLOWED_USERS) if ALLOWED_USERS else 'NONE'}")
 
+    # Start learning loop consumer in background if enabled
+    if _LEARNING_LOOP_ENABLED:
+        _start_learning_loop_background()
+    else:
+        print("Learning loop consumer disabled (set AIMS_LEARNING_LOOP_ENABLED=true to enable)")
+
     # Clear any webhook so long-polling works
     try:
         _api("deleteWebhook", {"drop_pending_updates": False})
diff --git a/ops/telegram/repairman_api.py b/ops/telegram/repairman_api.py
index c3e507b..d7e5954 100644
--- a/ops/telegram/repairman_api.py
+++ b/ops/telegram/repairman_api.py
@@ -6,12 +6,14 @@ Port: 8010
 """
 from __future__ import annotations
 
+import json
 import logging
 import os
 import pathlib
 import subprocess
 import threading
 import time
+import uuid
 from datetime import datetime
 
 from fastapi import Depends, FastAPI, HTTPException
@@ -37,6 +39,8 @@ app = FastAPI(title="RepairmanAPI", version="1.0.0")
 
 ISSUES_DIR.mkdir(parents=True, exist_ok=True)
 LOGS_DIR.mkdir(parents=True, exist_ok=True)
+PROJECT_ROOT = pathlib.Path(os.getenv("AIMS_REPO_DIR", str(ROOT))).resolve()
+DATA_ROOT = pathlib.Path(os.getenv("AIMS_WORKSPACE", str(PROJECT_ROOT / "aims_workspace"))).resolve()
 
 
 # ── Request/Response models ────────────────────────────────────────────────
@@ -45,6 +49,8 @@ class RepairmanTaskRequest(BaseModel):
     task: str
     mode: str = "repair"  # inspect | repair | fix
     source: str = "logi_orchestrator"  # logi_orchestrator | argus_agent | manual
+    validation_command: list[str] = []
+    rollback_command: list[str] = []
 
 
 class RepairmanTaskResponse(BaseModel):
@@ -70,7 +76,7 @@ class StatusResponse(BaseModel):
 async def trigger_task(req: RepairmanTaskRequest, _auth: None = Depends(verify_service_token)):
     """Trigger a repairman task (automated or manual).
 
-    Requires PoliAgent approval before execution.
+    Requires approval from the active SecurityAgent review gate.
     """
 
     # Check automation policy
@@ -80,41 +86,46 @@ async def trigger_task(req: RepairmanTaskRequest, _auth: None = Depends(verify_s
         raise HTTPException(status_code=403, detail="repair mode not allowed in automation policy")
     if req.mode == "fix" and not AUTO_FIX:
         raise HTTPException(status_code=403, detail="fix mode not allowed in automation policy")
+    if req.mode in {"repair", "fix"}:
+        if not req.validation_command:
+            raise HTTPException(
+                status_code=422,
+                detail="validation_command is required for modifying repair modes",
+            )
 
-    # Request PoliAgent approval
+    # Request active SecurityAgent approval
     import httpx
-    poli_url = os.getenv("POLI_URL", "http://poli-agent:8004") + "/check"
-
-    # Map mode to action_type
-    action_type_map = {
-        "inspect": "repairman_inspect",
-        "repair": "repairman_repair",
-        "fix": "repairman_fix",
-    }
-    action_type = action_type_map.get(req.mode, "repairman_repair")
+    policy_url = os.getenv(
+        "REPAIR_POLICY_GATE_URL",
+        "http://security-agent:8012/review",
+    )
 
-    # Build policy check request
+    risk_map = {"inspect": "low", "repair": "medium", "fix": "high"}
     policy_check = {
-        "action_type": action_type,
-        "params": {
-            "task": req.task,
-            "mode": req.mode,
-            "concept_preserved": True,  # Repairman always preserves concept
-            "restorative": True,        # Repairman changes are restorative
-        },
-        "requester": req.source,
-        "department": "self_healing",
+        "proposal_id": f"repairman-{int(time.time())}",
+        "files_changed": [],
+        "patch_diff": "",
+        "risk_level": risk_map.get(req.mode, "medium"),
+        "root_cause": req.task,
+        "restorative": True,
+        "concept_preserved": True,
+        "test_result": "not_run",
+        "rollback_notes": "Repairman must preserve a reversible patch and validation evidence.",
     }
 
     try:
-        from core.service_auth import service_headers
+        review_token = os.getenv("AIMS_CLAUDE_PROXY_TOKEN", "").strip()
+        review_headers = (
+            {"Authorization": f"Bearer {review_token}"} if review_token else {}
+        )
         async with httpx.AsyncClient(timeout=10.0) as client:
-            resp = await client.post(poli_url, json=policy_check, headers=service_headers())
+            resp = await client.post(policy_url, json=policy_check, headers=review_headers)
+            resp.raise_for_status()
             policy_result = resp.json()
 
-        if not policy_result.get("allowed", False):
-            reason = policy_result.get("reason", "unknown")
-            log.warning("PoliAgent denied repairman task: %s", reason)
+        if not policy_result.get("approved", False):
+            reason = "; ".join(policy_result.get("concerns", [])) or "unknown"
+            log.warning("SecurityAgent denied repairman task: %s", reason)
 
             # Send notification to Telegram if bot token is available
             try:
@@ -126,7 +137,7 @@ async def trigger_task(req: RepairmanTaskRequest, _auth: None = Depends(verify_s
                     import urllib.request
 
                     proposal_message = (
-                        f"🚫 PoliAgent denied automated repair\n\n"
+                        f"SecurityAgent denied automated repair\n\n"
                         f"Task: {req.task[:200]}\n"
                         f"Mode: {req.mode}\n"
                         f"Source: {req.source}\n"
@@ -156,15 +167,15 @@ async def trigger_task(req: RepairmanTaskRequest, _auth: None = Depends(verify_s
 
             raise HTTPException(
                 status_code=403,
-                detail=f"PoliAgent denied: {reason}"
+                detail=f"SecurityAgent denied: {reason}"
             )
 
-        audit_id = policy_result.get("audit_id", "unknown")
-        log.info("PoliAgent approved repairman task: audit_id=%s", audit_id)
+        audit_id = policy_result.get("review_id", "unknown")
+        log.info("SecurityAgent approved repairman task: review_id=%s", audit_id)
 
     except httpx.HTTPError as e:
-        log.error("Failed to contact PoliAgent: %s", e)
-        raise HTTPException(status_code=503, detail=f"PoliAgent unavailable: {e}")
+        log.error("Failed to contact SecurityAgent: %s", e)
+        raise HTTPException(status_code=503, detail=f"SecurityAgent unavailable: {e}")
 
     if not RUN_LOCK.acquire(blocking=False):
         return RepairmanTaskResponse(
@@ -181,14 +192,14 @@ async def trigger_task(req: RepairmanTaskRequest, _auth: None = Depends(verify_s
                 "You have explicit permission to modify project files inside /home/axi_omi_sphere/aims-workspace as needed for this task.\n"
                 "Goal: Restore process operability without changing concept.\n"
                 "Do not modify secrets, credentials, databases, production data, model files, external storage, deploy, git push, or restart services unless separately explicitly requested.\n\n"
-                f"PoliAgent audit_id: {audit_id}\n\n"
+                f"SecurityAgent review_id: {audit_id}\n\n"
                 "Task:\n" + req.task
             )
         else:  # repair
             full_task = (
                 "Run as AIMS Claude Code Repairman. Follow the repairman permission model.\n"
                 "Concept review passed. Production process output must remain unchanged.\n\n"
-                f"PoliAgent audit_id: {audit_id}\n\n"
+                f"SecurityAgent review_id: {audit_id}\n\n"
                 "Task:\n" + req.task
             )
 
@@ -205,21 +216,49 @@ async def trigger_task(req: RepairmanTaskRequest, _auth: None = Depends(verify_s
                 env["TERM"] = env.get("TERM", "xterm")
 
                 started = time.time()
-                log.info("DEBUG: RUN_REPAIRMAN=%s ROOT=%s exists=%s", RUN_REPAIRMAN, ROOT, RUN_REPAIRMAN.exists())
-                proc = subprocess.run(
-                    [str(RUN_REPAIRMAN), str(ROOT), str(issue_path)],
-                    cwd=str(ROOT),
-                    env=env,
-                    text=True,
-                    stdout=subprocess.PIPE,
-                    stderr=subprocess.STDOUT,
-                    timeout=3600,
-                )
-                elapsed = round(time.time() - started, 1)
-                output = proc.stdout or ""
-                log_path.write_text(output, encoding="utf-8", errors="replace")
+                if req.mode == "inspect":
+                    proc = subprocess.run(
+                        [str(RUN_REPAIRMAN), str(ROOT), str(issue_path)],
+                        cwd=str(ROOT),
+                        env=env,
+                        text=True,
+                        stdout=subprocess.PIPE,
+                        stderr=subprocess.STDOUT,
+                        timeout=3600,
+                    )
+                    output = proc.stdout or ""
+                    log_path.write_text(output, encoding="utf-8", errors="replace")
+                    lifecycle_status = "PASS" if proc.returncode == 0 else "FAIL"
+                else:
+                    from repairman.api_closed_loop import run_closed_repair
+
+                    lifecycle_id = f"repair_api_{uuid.uuid4().hex[:12]}"
+                    lifecycle_dir = (
+                        DATA_ROOT / "repairman_closed_loop" / lifecycle_id
+                    )
+                    lifecycle = run_closed_repair(
+                        task_id=lifecycle_id,
+                        owner_agent=req.source,
+                        run_dir=lifecycle_dir,
+                        cwd=PROJECT_ROOT,
+                        repair_command=[str(RUN_REPAIRMAN), str(PROJECT_ROOT), str(issue_path)],
+                        validation_command=list(req.validation_command),
+                        rollback_command=list(req.rollback_command),
+                        memory_dir=DATA_ROOT / "repairman_memory",
+                    )
+                    log_path.write_text(
+                        json.dumps(lifecycle, indent=2, ensure_ascii=False),
+                        encoding="utf-8",
+                    )
+                    lifecycle_status = lifecycle.get("status", "UNKNOWN")
 
-                log.info("Repairman task finished: exit=%d elapsed=%.1fs audit_id=%s", proc.returncode, elapsed, audit_id)
+                elapsed = round(time.time() - started, 1)
+                log.info(
+                    "Repairman task finished: lifecycle_status=%s elapsed=%.1fs review_id=%s",
+                    lifecycle_status,
+                    elapsed,
+                    audit_id,
+                )
             except Exception as e:
                 log.exception("Repairman task error: %s", e)
             finally:
diff --git a/ops/tests/test_axi_ai_label_removal_routing.py b/ops/tests/test_axi_ai_label_removal_routing.py
index 1bbaaa1..81f0f04 100644
--- a/ops/tests/test_axi_ai_label_removal_routing.py
+++ b/ops/tests/test_axi_ai_label_removal_routing.py
@@ -14,11 +14,13 @@ def _load_helpers():
     src = Path(__file__).resolve().parents[1] / "axi_bot.py"
     tree = ast.parse(src.read_text(encoding="utf-8"))
     wanted = {
+        "_is_docsreg_launch_text",
+        "_wants_docx",
         "_should_route_ai_label_removal_text",
         "_should_route_ai_label_removal_upload",
     }
 
-    blocks: list[str] = ["import time\n\n"]
+    blocks: list[str] = ["import re\nimport time\n\n"]
     for node in tree.body:
         if isinstance(node, ast.FunctionDef) and node.name in wanted:
             blocks.append(ast.get_source_segment(src.read_text(encoding="utf-8"), node) or "")
@@ -33,6 +35,7 @@ def _load_helpers():
 
 
 _helpers = _load_helpers()
+_wants_docx = _helpers["_wants_docx"]
 _should_route_ai_label_removal_text = _helpers["_should_route_ai_label_removal_text"]
 _should_route_ai_label_removal_upload = _helpers["_should_route_ai_label_removal_upload"]
 
@@ -48,6 +51,13 @@ def test_text_intent_is_detected() -> None:
     assert _should_route_ai_label_removal_text("удали метки ИИ", 123)
 
 
+def test_doc_generation_text_is_not_routed_to_ai_label_removal() -> None:
+    _helpers["_PENDING_AI_LABEL_REMOVAL"].clear()
+    assert not _should_route_ai_label_removal_text("create AIMS policy framework", 123)
+    assert not _should_route_ai_label_removal_text("Axi, create AIMS policy framework", 123)
+    assert not _should_route_ai_label_removal_text("create AIMS policy framework document", 123)
+
+
 def test_upload_caption_intent_is_detected() -> None:
     _helpers["_PENDING_AI_LABEL_REMOVAL"].clear()
     msg = _Msg(caption="remove AI labels from this document")
diff --git a/ops/tests/test_axi_docsreg_delegation.py b/ops/tests/test_axi_docsreg_delegation.py
index 1f7c942..ff1bb1a 100644
--- a/ops/tests/test_axi_docsreg_delegation.py
+++ b/ops/tests/test_axi_docsreg_delegation.py
@@ -67,3 +67,9 @@ async def test_omi_owned_intent_detects_docsreg_semantics(monkeypatch) -> None:
 def test_regular_docx_requests_still_match() -> None:
     assert _wants_docx("Please generate a Word report from this analysis")
     assert _wants_standards_docx_result("Please update the Word document with the fixes")
+
+
+def test_policy_framework_requests_match_docx_generation() -> None:
+    assert _wants_docx("Axi, create AIMS policy framework")
+    assert _wants_docx("Create policy framework for AIMS")
+    assert _wants_docx("Prepare AIMS policy framework document")
diff --git a/ops/tests/test_context_grounded_document_generation.py b/ops/tests/test_context_grounded_document_generation.py
index 4c20c9b..87b8e77 100644
--- a/ops/tests/test_context_grounded_document_generation.py
+++ b/ops/tests/test_context_grounded_document_generation.py
@@ -28,6 +28,10 @@ from ops.agents.skills.context_grounded_document_generation import (
     validate_capsule_coverage,
     validate_shard_coverage,
 )
+from ops.docgen.document_architecture import (
+    build_requirement_graph,
+    validate_architecture_gate,
+)
 from ops import cyclic_doc_generation_pipeline as pipeline
 
 
@@ -110,6 +114,45 @@ def test_render_includes_full_similar_document_content() -> None:
     assert tail in render_generation_prompt(context)
 
 
+def test_render_uses_dynamic_section_map_and_requirement_contracts() -> None:
+    context = GenerationContext(
+        topic="Maintenance policy framework",
+        doc_type="policy_framework",
+        task_context="maintenance policy with backlog control",
+        standards=[],
+        reference=None,
+        templates=[],
+        similar_documents=[],
+        provenance=[],
+        warnings=[],
+        dynamic_section_map=[
+            "Document Control",
+            "Purpose and Objectives",
+            "Maintenance Management",
+        ],
+        requirement_graph={
+            "section_contracts": {
+                "Maintenance Management": {
+                    "required_subsections": ["Backlog control"],
+                    "implementation_requirements": ["Define responsible roles", "Define records"],
+                    "required_controls": ["preventive controls"],
+                    "required_records": ["process evidence"],
+                    "required_roles": ["accountable owner"],
+                }
+            }
+        },
+    )
+
+    sections = build_section_contract(context)
+    prompt = render_generation_prompt(context)
+
+    assert any(section.title == "Maintenance Management" for section in sections)
+    assert "REQUIREMENT GRAPH" in prompt
+    assert "PROCESS IMPLEMENTATION CONTRACTS" in prompt
+    assert "Required subsections: Backlog control" in prompt
+    assert "Implementation requirements: Define responsible roles, Define records" in prompt
+
+
 def test_slot120_sends_full_prompt_without_character_truncation(
     monkeypatch,
 ) -> None:
@@ -138,6 +181,8 @@ def test_slot120_sends_full_prompt_without_character_truncation(
 class _StreamResponse:
     def __init__(self, lines: list[bytes]):
         self.lines = lines
+        self.headers = {}
+        self.status = 200
 
     def __enter__(self):
         return self
@@ -148,6 +193,9 @@ class _StreamResponse:
     def __iter__(self):
         return iter(self.lines)
 
+    def read(self):
+        return b"".join(self.lines)
+
 
 def test_slot120_runtime_matches_current_ollama_profile() -> None:
     assert pipeline.SLOT120_NUM_CTX == 32768
@@ -174,23 +222,12 @@ def test_runtime_preflight_does_not_double_count_loaded_required_model(
     monkeypatch,
     tmp_path,
 ) -> None:
-    ollama_list = subprocess.CompletedProcess(
-        args=["ollama", "list"],
-        returncode=0,
-        stdout=(
-            "NAME ID SIZE MODIFIED\n"
-            "qwen25-chat-14-v19-new:latest abc 16 GB now\n"
-            "qwen36-reasoning-35b-v1:latest def 39 GB now\n"
-        ),
-        stderr="",
-    )
-    monkeypatch.setattr(
-        pipeline.subprocess,
-        "run",
-        lambda *args, **kwargs: ollama_list,
-    )
-
     class RuntimeResponse:
+        def __init__(self, payload: dict):
+            self.payload = payload
+            self.headers = {}
+            self.status = 200
+
         def __enter__(self):
             return self
 
@@ -198,25 +235,38 @@ def test_runtime_preflight_does_not_double_count_loaded_required_model(
             return False
 
         def read(self):
-            return json.dumps(
-                {
-                    "models": [
-                        {
-                            "name": "qwen36-reasoning-35b-v1:latest",
-                            "size_vram": 39 * 1024**3,
-                        },
-                        {
-                            "name": "unrelated-model:latest",
-                            "size_vram": 37 * 1024**3,
-                        },
-                    ]
-                }
-            ).encode()
+            return json.dumps(self.payload).encode()
 
     monkeypatch.setattr(
         pipeline.urllib.request,
         "urlopen",
-        lambda *args, **kwargs: RuntimeResponse(),
+        lambda req, *args, **kwargs: RuntimeResponse(
+            {
+                "models": [
+                    {
+                        "name": "qwen25-chat-14-v19:latest",
+                        "size": 16 * 1024**3,
+                    },
+                    {
+                        "name": "qwen36-reasoning-35b-v1:latest",
+                        "size": 39 * 1024**3,
+                    },
+                ]
+            }
+            if "/api/tags" in str(req)
+            else {
+                "models": [
+                    {
+                        "name": "qwen36-reasoning-35b-v1:latest",
+                        "size_vram": 39 * 1024**3,
+                    },
+                    {
+                        "name": "unrelated-model:latest",
+                        "size_vram": 37 * 1024**3,
+                    },
+                ]
+            }
+        ),
     )
 
     result = pipeline._runtime_preflight(tmp_path)
@@ -828,6 +878,136 @@ def test_capsule_prompt_contains_full_shard_and_strict_identity() -> None:
     assert "strict JSON only" in prompt
 
 
+def test_generation_context_records_selected_exemplar_and_diagnostics(monkeypatch) -> None:
+    candidates = [
+        ContextSource(
+            source_type="similar_document",
+            title="generic policy exemplar",
+            content="Approved policy exemplar content",
+            path="/tmp/exemplar-a.docx",
+            score=0.8,
+            metadata={"is_master": False},
+        ),
+        ContextSource(
+            source_type="similar_document",
+            title="master policy exemplar",
+            content="Approved master policy exemplar content",
+            path="/tmp/exemplar-b.docx",
+            score=0.7,
+            metadata={"is_master": True},
+        ),
+    ]
+    monkeypatch.setattr(
+        "ops.agents.skills.context_grounded_document_generation._similar_registry_documents",
+        lambda *args, **kwargs: candidates,
+    )
+    monkeypatch.setattr(
+        "ops.agents.skills.context_grounded_document_generation._registry_path",
+        lambda: "/tmp/aims_registry.db",
+    )
+
+    context = build_generation_context(
+        topic="Create AIMS policy framework",
+        doc_type="policy_framework",
+        task_context="maintenance and governance",
+        similar_limit=2,
+    )
+
+    assert context.selected_exemplar is not None
+    assert context.selected_exemplar.title == "master policy exemplar"
+    assert context.retrieval_diagnostics["retrieval_passed"] is True
+    assert context.retrieval_diagnostics["candidate_count"] == 2
+
+
+def test_strict_retrieval_blocks_when_expected_exemplar_is_missing(monkeypatch) -> None:
+    monkeypatch.setattr(
+        "ops.agents.skills.context_grounded_document_generation._similar_registry_documents",
+        lambda *args, **kwargs: [],
+    )
+    monkeypatch.setattr(
+        "ops.agents.skills.context_grounded_document_generation._registry_path",
+        lambda: "/tmp/aims_registry.db",
+    )
+
+    with pytest.raises(RuntimeError, match="EXPECTED_EXEMPLAR_NOT_RETRIEVED"):
+        build_generation_context(
+            topic="Create AIMS policy framework",
+            doc_type="policy_framework",
+            task_context="maintenance and governance",
+            similar_limit=2,
+            strict_retrieval=True,
+        )
+
+
+def test_maintenance_context_expands_final_section_map_before_skeleton_generation() -> None:
+    context = GenerationContext(
+        topic="Maintenance policy framework",
+        doc_type="policy_framework",
+        task_context="maintenance obligations and backlog management",
+        standards=[],
+        reference=None,
+        templates=[],
+        similar_documents=[],
+        selected_exemplar=None,
+        provenance=[],
+        warnings=[],
+    )
+    source = "The document shall define maintenance governance and backlog management."
+    plan = semantic_split(source, source_id="maintenance", max_chars=1000)
+    shard = plan.shards[0]
+    batch = parse_requirement_capsule_batch(
+        shard,
+        _capsule_payload(
+            shard,
+            statement="The document shall define maintenance governance and backlog management.",
+            evidence="maintenance governance and backlog management",
+            section_hint="Maintenance Management",
+        ),
+        source_type="reference",
+    )
+    catalog = build_requirement_catalog([(plan, [batch])], catalog_id="maintenance-test")
+
+    from ops.docgen.document_architecture import expand_section_map
+    from ops.docgen.document_architecture.domain_requirement_extractor import BASE_POLICY_ARCHETYPE
+
+    graph = build_requirement_graph(
+        request_text=context.task_context,
+        document_type=context.doc_type,
+        retrieved_documents=[],
+        selected_exemplar=None,
+        retrieval_diagnostics={"retrieval_passed": False},
+    )
+    expanded = expand_section_map(BASE_POLICY_ARCHETYPE, graph)
+    assert "Maintenance Management" in expanded
+
+
+def test_architecture_gate_blocks_when_retrieval_is_missing() -> None:
+    gate = validate_architecture_gate(
+        retrieval_passed=False,
+        expected_exemplar_found=False,
+        requirement_count=0,
+        final_section_map=[],
+    )
+
+    assert not gate.passed
+    assert "EXPECTED_EXEMPLAR_NOT_RETRIEVED" in gate.blockers
+
+
+def test_requirement_graph_uses_selected_exemplar_and_context_overlay() -> None:
+    graph = build_requirement_graph(
+        request_text="Create AIMS policy framework with maintenance obligations.",
+        document_type="policy_framework",
+        retrieved_documents=[
+            {"title": "master policy exemplar", "summary": "Approved framework"},
+        ],
+        selected_exemplar={"title": "master policy exemplar", "score": 1.0},
+        retrieval_diagnostics={"retrieval_passed": True},
+    )
+
+    assert graph.selected_exemplar["title"] == "master policy exemplar"
+    assert "Maintenance Management" in graph.final_section_map
+
+
 def test_candidate_bundle_builds_and_generates_sections_deterministically() -> None:
     context = GenerationContext(
         topic="Asset Integrity Management Policy and Framework",
diff --git a/ops/tests/test_cyclic_doc_hard_gate.py b/ops/tests/test_cyclic_doc_hard_gate.py
index 0cb8b38..f91ab52 100644
--- a/ops/tests/test_cyclic_doc_hard_gate.py
+++ b/ops/tests/test_cyclic_doc_hard_gate.py
@@ -55,6 +55,23 @@ def test_hard_gate_fails_closed_for_each_required_signal() -> None:
     ]
 
 
+def test_hard_gate_uses_message_when_reason_is_absent() -> None:
+    gate = SimpleNamespace(allowed=False, message="BLOCKED_BY_NEW_GATE")
+
+    allowed, failures = _evaluate_cycle_hard_gate(
+        gate=gate,
+        struct_report=_structure(),
+        audit_schema_passed=True,
+        audit_quality_passed=True,
+        audit_quality_failures=[],
+        rec_lineage_passed=True,
+        critical_regression=False,
+    )
+
+    assert allowed is False
+    assert failures == ["BLOCKED_BY_NEW_GATE"]
+
+
 def test_hard_gate_rejects_valid_but_low_quality_audit() -> None:
     allowed, failures = _evaluate_cycle_hard_gate(
         gate=_gate(),
diff --git a/ops/tests/test_cyclic_doc_phase16_enforcement.py b/ops/tests/test_cyclic_doc_phase16_enforcement.py
index 36b9ac1..f5f13a3 100644
--- a/ops/tests/test_cyclic_doc_phase16_enforcement.py
+++ b/ops/tests/test_cyclic_doc_phase16_enforcement.py
@@ -1,7 +1,12 @@
+import json
+from pathlib import Path
 from types import SimpleNamespace
 
+import pytest
+
 from ops.cyclic_doc_generation_pipeline import (
     _learning_pair_export_decision,
+    _slot14_route_topic,
 )
 from ops.docgen.universal_overlay.enforcement_contracts import (
     EnforcementContext,
@@ -171,3 +176,20 @@ def test_learning_pair_export_requires_all_gates():
     )
     assert allowed is False
     assert failures == ["hard_gate=FAIL"]
+
+
+def test_slot14_route_topic_falls_back_when_router_unavailable(monkeypatch, tmp_path: Path):
+    def _raise(*args, **kwargs):
+        raise ConnectionRefusedError(111, "Connection refused")
+
+    monkeypatch.setattr(
+        "ops.cyclic_doc_generation_pipeline.urllib.request.urlopen",
+        _raise,
+    )
+
+    routed = _slot14_route_topic("AIMS policy framework", tmp_path)
+
+    assert routed == "AIMS policy framework"
+    payload = json.loads((tmp_path / "slot14_route.json").read_text(encoding="utf-8"))
+    assert payload["status"] == "FALLBACK"
+    assert payload["fallback_topic"] == "AIMS policy framework"
diff --git a/ops/tests/test_docgen_document_type_profile_loader.py b/ops/tests/test_docgen_document_type_profile_loader.py
index 0351f4b..d251cb5 100644
--- a/ops/tests/test_docgen_document_type_profile_loader.py
+++ b/ops/tests/test_docgen_document_type_profile_loader.py
@@ -35,6 +35,31 @@ def test_policy_profile_blocks_technical_report_structure() -> None:
     assert profile.generation_contract()["forbidden_references"] == []
 
 
+def test_policy_profile_is_generic_with_only_baseline_formation_standards() -> None:
+    profile = get_document_generation_profile("policy_framework")
+
+    assert "Document Control" in profile.required_sections
+    assert "Framework Overview" not in profile.required_sections
+    assert "Lifecycle Coverage" not in profile.required_sections
+    assert "Governance and Decision Rights" not in profile.required_sections
+    assert "Risk Management" not in profile.required_sections
+    assert "Records and Retention" in profile.required_sections
+    assert "References and Related Documents" in profile.required_sections
+    assert "Annexures" in profile.required_sections
+    assert profile.standards.formation == (
+        "ISO 9001:2015",
+        "ISO 10013:2021",
+        "ISO 2145:1978",
+        "ISO 690:2021",
+    )
+    assert profile.standards.discovery_candidates == (
+        "ISO 31000",
+        "ISO 37000",
+        "ISO 37301",
+        "ISO 19011",
+    )
+
+
 def test_inspection_report_profile_includes_governing_discovery_candidate() -> None:
     profile = get_document_generation_profile("inspection_report")
 
diff --git a/ops/tests/test_docgen_improvement_policy.py b/ops/tests/test_docgen_improvement_policy.py
index 234323e..3d5a0bd 100644
--- a/ops/tests/test_docgen_improvement_policy.py
+++ b/ops/tests/test_docgen_improvement_policy.py
@@ -19,6 +19,27 @@ def _seed_cycle(cycle, quality, hard_gate=True, profile_status="PASS", repair_se
     _write_json(cycle / "repair_plan.json", {"selected_count": repair_selected})
 
 
+def test_cycle_improvement_candidates_is_not_counted_as_attempt(tmp_path):
+    run_dir = tmp_path / "run"
+    _seed_cycle(run_dir / "cycle_01", 0.81)
+    _seed_cycle(run_dir / "cycle_02", 0.84)
+    _seed_cycle(run_dir / "cycle_03", 0.86)
+    _seed_cycle(run_dir / "cycle_improvement_candidates", 0.5)
+
+    decision = evaluate_improvement_policy(
+        run_dir=run_dir,
+        document_type="technical_report",
+        audit_dir=tmp_path / "audit",
+    )
+
+    assert decision.observed_attempts == 3
+    assert [attempt.cycle for attempt in decision.attempts] == [
+        "cycle_01",
+        "cycle_02",
+        "cycle_03",
+    ]
+
+
 def test_first_cycle_triggers_immediate_analysis(tmp_path):
     run_dir = tmp_path / "run"
     _seed_cycle(run_dir / "cycle_01", 0.83)
diff --git a/ops/tests/test_docgen_nightly_improvement_orchestrator.py b/ops/tests/test_docgen_nightly_improvement_orchestrator.py
index 7a57b17..6a03e42 100644
--- a/ops/tests/test_docgen_nightly_improvement_orchestrator.py
+++ b/ops/tests/test_docgen_nightly_improvement_orchestrator.py
@@ -1,7 +1,17 @@
+import asyncio
 import json
+from types import SimpleNamespace
+
+import pytest
 
 from ops.docgen.universal_overlay.nightly_improvement_orchestrator import (
     build_docgen_nightly_batch_action,
+    build_docgen_upgrade_batch_action,
+    build_docgen_followup_batch_action,
+    register_docgen_followup_batch_action,
+    run_docgen_upgrade_batch_now,
+    stream_docgen_upgrade_progress,
+    write_docgen_orchestration_state,
     write_nightly_feedback,
 )
 
@@ -13,13 +23,24 @@ def test_build_docgen_nightly_batch_action_is_scheduler_ready():
     )
 
     assert action.action_id.startswith("PA-DOCGEN-NIGHTLY-")
-    assert action.command_or_callable == "python3 ops/scripts/docgen_branch_batch_nightly.py"
+    assert action.command_or_callable == "python3 scripts/docgen_branch_batch_nightly.py"
     assert action.command_args[:2] == ["--run-tag", "20260616T223000Z"]
     assert action.resource_policy["max_vram_gb"] == 0
     assert action.resource_policy["cpu_only_ok"] is True
     assert action.safety_policy["no_model_promotion"] is True
 
 
+def test_build_docgen_nightly_batch_action_can_disable_self_improvement():
+    action = build_docgen_nightly_batch_action(
+        document_types=["policy_framework"],
+        run_tag="20260616T223000Z",
+        self_improvement_enabled=False,
+    )
+
+    assert action.command_args[-1] == "--no-self-improvement"
+    assert "generation-only" in action.description
+
+
 def test_write_nightly_feedback_consumes_batch_report(tmp_path):
     report = tmp_path / "docgen_bulk_branch_run_20260616T223000Z.json"
     report.write_text(
@@ -37,4 +58,308 @@ def test_write_nightly_feedback_consumes_batch_report(tmp_path):
     assert feedback["branch_count"] == 2
     assert feedback["error_count"] == 1
     assert feedback["status"] == "REVIEW"
+    assert feedback["decision"]["dominant_issue"] == "integration_and_stability"
+    assert "integration" in feedback["decision"]["recommended_update_types"]
+    assert feedback["decision"]["next_orchestrator_step"] == "analyze_failure_material"
+    assert feedback["followup_action"]["title"] == "DOCGEN nightly follow-up batch"
     assert (tmp_path / "feedback.json").exists()
+
+
+def test_write_nightly_feedback_can_disable_followup(tmp_path):
+    report = tmp_path / "docgen_bulk_branch_run_20260616T223000Z.json"
+    report.write_text(
+        json.dumps(
+            [
+                {"document_type": "policy_framework", "convergence_score": 0.93},
+            ]
+        ),
+        encoding="utf-8",
+    )
+
+    feedback = write_nightly_feedback(
+        report_path=report,
+        feedback_path=tmp_path / "feedback.json",
+        self_improvement_enabled=False,
+    )
+
+    assert feedback["followup_action"] is None
+
+
+def test_write_nightly_feedback_detects_quality_plateau(tmp_path, monkeypatch):
+    import ops.docgen.universal_overlay.nightly_improvement_orchestrator as orchestrator
+
+    monkeypatch.setattr(orchestrator, "STATUS_DIR", tmp_path)
+
+    previous = tmp_path / "docgen_nightly_feedback_previous.json"
+    previous.write_text(
+        json.dumps(
+            {
+                "avg_quality": 0.931,
+            }
+        ),
+        encoding="utf-8",
+    )
+
+    report = tmp_path / "docgen_bulk_branch_run_20260617T010000Z.json"
+    report.write_text(
+        json.dumps(
+            [
+                {"document_type": "policy_framework", "convergence_score": 0.936},
+                {"document_type": "technical_report", "convergence_score": 0.938},
+            ]
+        ),
+        encoding="utf-8",
+    )
+
+    feedback = write_nightly_feedback(report_path=report, feedback_path=tmp_path / "feedback.json")
+
+    assert feedback["decision"]["training_review_required"] is True
+    assert feedback["decision"]["next_orchestrator_step"] == "prepare_training_review_packet"
+    assert "model_review" in feedback["decision"]["recommended_update_types"]
+
+
+def test_write_orchestration_state_consumes_feedback_decision(tmp_path):
+    run_dir = tmp_path / "run"
+    cycle = run_dir / "cycle_01"
+    cycle.mkdir(parents=True)
+    (cycle / "metrics.json").write_text(json.dumps({"overall_score": 0.93}), encoding="utf-8")
+    (cycle / "skills_report.json").write_text(json.dumps({"hard_gate_allowed": False}), encoding="utf-8")
+    (cycle / "document_profile_conformance.json").write_text(json.dumps({"status": "REVIEW"}), encoding="utf-8")
+    (cycle / "repair_plan.json").write_text(json.dumps({"selected_count": 1}), encoding="utf-8")
+
+    feedback_path = tmp_path / "feedback.json"
+    feedback_path.write_text(
+        json.dumps(
+            {
+                "decision": {
+                    "recommended_updates": ["ops/docgen/universal_overlay/docgen_chain_audit.py"],
+                    "next_orchestrator_step": "analyze_failure_material",
+                }
+            }
+        ),
+        encoding="utf-8",
+    )
+
+    orchestration = write_docgen_orchestration_state(
+        run_dir=run_dir,
+        document_type="policy_framework",
+        feedback_path=feedback_path,
+    )
+
+    assert orchestration["feedback_decision"]["next_orchestrator_step"] == "analyze_failure_material"
+    assert "ops/docgen/universal_overlay/docgen_chain_audit.py" in orchestration["recommended_updates"]
+    assert orchestration["feedback_followup_action"] is None
+
+
+def test_write_orchestration_state_generation_only_mode(tmp_path):
+    run_dir = tmp_path / "run"
+    cycle = run_dir / "cycle_01"
+    cycle.mkdir(parents=True)
+    (cycle / "metrics.json").write_text(json.dumps({"overall_score": 0.93}), encoding="utf-8")
+    (cycle / "skills_report.json").write_text(json.dumps({"hard_gate_allowed": False}), encoding="utf-8")
+    (cycle / "document_profile_conformance.json").write_text(json.dumps({"status": "REVIEW"}), encoding="utf-8")
+    (cycle / "repair_plan.json").write_text(json.dumps({"selected_count": 1}), encoding="utf-8")
+
+    orchestration = write_docgen_orchestration_state(
+        run_dir=run_dir,
+        document_type="policy_framework",
+        self_improvement_enabled=False,
+    )
+
+    assert orchestration["next_step"] == "report_generation_only"
+    assert orchestration["scheduling_required"] is False
+    assert orchestration["candidate_action"] is None
+    assert orchestration["followup_contract"] is None
+
+
+def test_build_followup_batch_action_uses_feedback_branch_types():
+    action = build_docgen_followup_batch_action(
+        report_path="/tmp/docgen_bulk_branch_run_20260616T223000Z.json",
+        feedback={
+            "decision": {
+                "branch_types": ["policy_framework", "technical_report"],
+                "dominant_issue": "integration_and_stability",
+                "recommended_update_types": ["integration", "stability"],
+                "next_orchestrator_step": "analyze_failure_material",
+            }
+        },
+    )
+
+    assert action.action_id.startswith("PA-DOCGEN-NIGHTLY-")
+    assert action.title == "DOCGEN nightly follow-up batch"
+    assert action.command_args[0] == "--run-tag"
+    assert "docgen_bulk_branch_run_20260616T223000Z_followup" in action.command_args[1]
+    assert action.command_args[-1] == "--self-improvement-enabled"
+    assert "policy_framework" in action.command_args
+    assert "technical_report" in action.command_args
+    assert "integration" in action.description
+
+
+def test_register_followup_action_invokes_registry(monkeypatch):
+    captured = {}
+
+    class DummyRegistry:
+        def register(self, action):
+            captured["action_id"] = action.action_id
+            captured["title"] = action.title
+            return None
+
+    monkeypatch.setattr(
+        "ops.docgen.universal_overlay.nightly_improvement_orchestrator.StrategicPlannedActionsRegistry",
+        lambda: DummyRegistry(),
+    )
+
+    result = register_docgen_followup_batch_action(
+        report_path="/tmp/docgen_bulk_branch_run_20260616T223000Z.json",
+        feedback={
+            "decision": {
+                "branch_types": ["policy_framework"],
+                "dominant_issue": "integration_and_stability",
+                "recommended_update_types": ["integration", "stability"],
+                "next_orchestrator_step": "analyze_failure_material",
+            }
+        },
+    )
+
+    assert captured["title"] == "DOCGEN nightly follow-up batch"
+    assert result["action"]["title"] == "DOCGEN nightly follow-up batch"
+
+
+def test_run_docgen_upgrade_batch_now_executes_immediately(monkeypatch):
+    calls = {}
+
+    class DummyRegistry:
+        def register(self, action):
+            calls["registered"] = action.action_id
+
+    class DummyRunner:
+        def __init__(self, mode):
+            calls["mode"] = mode
+
+        def run(self, action):
+            calls["ran"] = action.action_id
+            return {"status": "PASS", "exit_code": 0}
+
+    monkeypatch.setattr(
+        "ops.docgen.universal_overlay.nightly_improvement_orchestrator.StrategicPlannedActionsRegistry",
+        lambda: DummyRegistry(),
+    )
+    monkeypatch.setattr(
+        "ops.docgen.universal_overlay.nightly_improvement_orchestrator.PlannedActionRunner",
+        DummyRunner,
+    )
+    monkeypatch.setattr(
+        "ops.docgen.universal_overlay.nightly_improvement_orchestrator._summarize_docgen_upgrade_run",
+        lambda **kwargs: {
+            "loop_state": "IN_PROGRESS",
+            "shelf_state": "repair_queue",
+            "next_step": "analyze_failure_material",
+            "target_quality": 0.98,
+            "best_quality": 0.934,
+            "best_cycles": 38,
+            "avg_quality": 0.921,
+            "trajectory_trace": "93.0% -> 96.0% -> 93.4%",
+            "batch_summaries": [
+                {
+                    "document_type": "policy_framework",
+                    "achieved_quality": 0.934,
+                    "cycles_completed": 38,
+                }
+            ],
+        },
+    )
+
+    result = run_docgen_upgrade_batch_now(
+        document_types=["policy_framework"],
+    )
+
+    assert calls["registered"] == calls["ran"]
+    assert calls["mode"] == "live-lightweight"
+    assert result["executed_now"] is True
+    assert result["execution_result"]["status"] == "PASS"
+    assert result["run_summary"]["best_cycles"] == 38
+    assert result["run_summary"]["loop_state"] == "IN_PROGRESS"
+
+
+@pytest.mark.asyncio
+async def test_stream_docgen_upgrade_progress_silent_until_cycle_complete(monkeypatch, tmp_path):
+    run_root = tmp_path / "runs"
+    run_dir = run_root / "bulk_policy_framework_docgen_upgrade_20260618T112100Z"
+    run_dir.mkdir(parents=True)
+    (run_dir / "summary.json").write_text(
+        json.dumps(
+            {
+                "cycles_completed": 0,
+                "achieved_quality": 0.0,
+                "status": "IN_PROGRESS",
+            }
+        ),
+        encoding="utf-8",
+    )
+    run_root.mkdir(exist_ok=True)
+    action = SimpleNamespace(
+        action_id="PA-DOCGEN-UPGRADE-docgen_upgrade_20260618T112100Z",
+        command_args=["--run-tag", "docgen_upgrade_20260618T112100Z"],
+    )
+    snapshots = [
+        SimpleNamespace(stage_state="PREFLIGHT_READY", total_artifacts=6),
+        SimpleNamespace(stage_state="PREFLIGHT_READY", total_artifacts=11),
+    ]
+    snapshot_calls = []
+    sent_messages = []
+
+    def fake_snapshot(**_kwargs):
+        index = min(len(snapshot_calls), len(snapshots) - 1)
+        snapshot_calls.append(index)
+        return snapshots[index]
+
+    class FakeBot:
+        async def send_message(self, chat_id, text, parse_mode=None):
+            sent_messages.append((chat_id, text))
+            return SimpleNamespace(message_id=len(sent_messages))
+
+    monkeypatch.setattr(
+        "ops.docgen.universal_overlay.nightly_improvement_orchestrator._docgen_upgrade_run_root",
+        lambda **_kwargs: run_root,
+    )
+    monkeypatch.setattr(
+        "ops.docgen.universal_overlay.docgen_progress_watchdog.collect_watchdog_snapshot",
+        fake_snapshot,
+    )
+
+    stop_event = asyncio.Event()
+
+    async def _stop_soon():
+        await asyncio.sleep(0.05)
+        stop_event.set()
+
+    stopper = asyncio.create_task(_stop_soon())
+    try:
+        await stream_docgen_upgrade_progress(
+            action=action,
+            document_types=["policy_framework"],
+            chat_id=123,
+            bot=FakeBot(),
+            stop_event=stop_event,
+            poll_interval_seconds=0.01,
+        )
+    finally:
+        stopper.cancel()
+
+    assert sent_messages == []
+
+
+def test_build_docgen_upgrade_batch_action_uses_upgrade_prefix():
+    action = build_docgen_upgrade_batch_action(
+        document_types=["policy_framework"],
+        self_improvement_enabled=True,
+        topic_prefix="AIMS",
+        max_cycles=0,
+    )
+
+    assert action.action_id.startswith("PA-DOCGEN-UPGRADE-")
+    assert action.title == "DOCGEN upgrade batch"
+    assert "--max-cycles" in action.command_args
+    assert "0" in action.command_args
+    assert "--topic-prefix" in action.command_args
+    assert "AIMS" in action.command_args
diff --git a/ops/tests/test_docgen_standards_catalog.py b/ops/tests/test_docgen_standards_catalog.py
index 3a94326..0f9766f 100644
--- a/ops/tests/test_docgen_standards_catalog.py
+++ b/ops/tests/test_docgen_standards_catalog.py
@@ -75,6 +75,13 @@ def test_all_requested_document_types_have_mapping() -> None:
 
     assert expected.issubset(DOCUMENT_TYPE_STANDARD_MAP)
     assert "ISO 10013:2021" in standards_for_document_type("method_statement")
+    policy_standards = set(standards_for_document_type("policy_framework"))
+    assert {
+        "ISO 9001:2015",
+        "ISO 10013:2021",
+        "ISO 2145:1978",
+        "ISO 690:2021",
+    }.issubset(policy_standards)
 
 
 def test_catalog_contains_contextual_inspection_standards() -> None:
diff --git a/ops/tests/test_docgen_universal_overlay_backend_wrapper.py b/ops/tests/test_docgen_universal_overlay_backend_wrapper.py
index 67e279c..427da05 100644
--- a/ops/tests/test_docgen_universal_overlay_backend_wrapper.py
+++ b/ops/tests/test_docgen_universal_overlay_backend_wrapper.py
@@ -84,3 +84,31 @@ def test_backend_wrapper_reraises_original_exception(monkeypatch, tmp_path):
         (tmp_path / "universal_overlay" / "backend_result.json").read_text()
     )
     assert saved["status"] == "BACKEND_FAILED"
+
+
+def test_backend_wrapper_can_disable_self_improvement(monkeypatch, tmp_path):
+    monkeypatch.setattr(
+        "ops.docgen.universal_overlay.lifecycle_adapter.capture_model_runtime_snapshot",
+        _fake_snapshot,
+    )
+
+    result = run_backend_with_overlay(
+        backend=lambda output_dir: _Result(score=0.81),
+        backend_kwargs={"output_dir": tmp_path},
+        output_dir=tmp_path,
+        document_type="maintenance_procedure",
+        self_improvement_enabled=False,
+    )
+
+    assert result.score == 0.81
+    saved = json.loads(
+        (tmp_path / "universal_overlay" / "backend_result.json").read_text()
+    )
+    assert saved["status"] == "BACKEND_COMPLETED"
+    assert saved["self_improvement_enabled"] is False
+    handoff = saved["self_improvement_handoff"]
+    assert handoff["training_queue_manifest"].endswith("training_queue_manifest.json")
+    manifest = json.loads(
+        (tmp_path / "universal_overlay" / "training_queue_manifest.json").read_text()
+    )
+    assert manifest["status"] == "SELF_IMPROVEMENT_DISABLED"
diff --git a/ops/tests/test_docgen_universal_overlay_self_improvement_handoff.py b/ops/tests/test_docgen_universal_overlay_self_improvement_handoff.py
index ee740c1..dd287f1 100644
--- a/ops/tests/test_docgen_universal_overlay_self_improvement_handoff.py
+++ b/ops/tests/test_docgen_universal_overlay_self_improvement_handoff.py
@@ -89,6 +89,9 @@ def test_handoff_marks_repeated_skill_as_candidate_and_verified_repair(tmp_path)
 
     assert skills["requests"][0]["lifecycle_state"] == "CANDIDATE_SKILL"
     assert skills["requests"][0]["observed_count"] == 2
+    assert skills["requests"][0]["scope_state"] == "cross_branch_specific"
+    assert len(skills["requests"][0]["validation_plan"]) == 15
+    assert skills["requests"][0]["cross_branch_validation_required"] is True
     assert readiness["capabilities"]["repair_application_verified"] is True
     assert readiness["autonomous_self_improvement_ready"] is True
 
@@ -207,7 +210,7 @@ def test_handoff_writes_orchestration_state_for_nightly_follow_up(tmp_path):
     assert orchestration["followup_contract"]["owner_agent"] == "repairman"
     assert orchestration["followup_task_payload"]["agent"] == "repairman"
     assert orchestration["candidate_action"]["command_or_callable"] == (
-        "python3 ops/scripts/docgen_branch_batch_nightly.py"
+        "python3 scripts/docgen_branch_batch_nightly.py"
     )
     assert "slot32" in orchestration["candidate_action"]["resource_policy"][
         "forbidden_concurrent_slots"
@@ -215,3 +218,127 @@ def test_handoff_writes_orchestration_state_for_nightly_follow_up(tmp_path):
     assert "slot120" in orchestration["candidate_action"]["resource_policy"][
         "forbidden_concurrent_slots"
     ]
+
+
+def test_handoff_writes_training_material_index(tmp_path, monkeypatch):
+    run_dir = tmp_path / "run"
+    cycle = run_dir / "cycle_01"
+    cycle.mkdir(parents=True)
+    (cycle / "draft.md").write_text("# Policy Framework", encoding="utf-8")
+    _write(cycle / "metrics.json", {"overall_score": 0.91, "changes_applied": []})
+    _write(cycle / "document_profile_conformance.json", {"status": "REVIEW"})
+    _write(cycle / "skills_report.json", {"hard_gate_allowed": False})
+    _write(cycle / "training_quarantine.json", {"hard_gate_failures": []})
+    _write(cycle / "repair_plan.json", {"selected_count": 2})
+    _write(cycle / "section_failure_diagnostics.json", {"failures": []})
+
+    monkeypatch.setattr(
+        "ops.docgen.universal_overlay.self_improvement_handoff.write_training_material_index",
+        lambda output_path=None, root=None: {
+            "record_count": 2,
+            "by_slot": {"slot14": 1, "slot32": 1, "slot120": 0},
+            "training_tracks": {"slot14": 1, "slot32": 1, "slot120": 0},
+        },
+    )
+
+    outputs = create_self_improvement_handoff(
+        run_dir=run_dir,
+        document_type="policy_framework",
+    )
+
+    material_index = json.loads(outputs["training_material_index"].read_text())
+    readiness = json.loads(outputs["self_improvement_readiness"].read_text())
+
+    assert material_index["record_count"] == 2
+    assert readiness["training_material_index"]["record_count"] == 2
+    assert readiness["training_material_index"]["training_tracks"]["slot32"] == 1
+
+
+def test_handoff_writes_training_queue_manifest(tmp_path, monkeypatch):
+    run_dir = tmp_path / "run"
+    for index, changes in ((1, []), (2, ["repair"])):
+        cycle = run_dir / f"cycle_{index:02d}"
+        cycle.mkdir(parents=True)
+        (cycle / "draft.md").write_text("# Policy Framework", encoding="utf-8")
+        _write(cycle / "metrics.json", {"overall_score": 0.91 + index * 0.01, "changes_applied": changes})
+        _write(cycle / "document_profile_conformance.json", {"status": "REVIEW"})
+        _write(cycle / "skills_report.json", {"hard_gate_allowed": False})
+        _write(cycle / "training_quarantine.json", {"hard_gate_failures": []})
+        _write(cycle / "repair_plan.json", {"selected_count": 2})
+        _write(cycle / "section_failure_diagnostics.json", {"failures": [
+            {
+                "stage": "generation",
+                "cause": "EMPTY_SECTION",
+                "section": "3.1",
+                "evidence": "missing body",
+                "next_action": "regenerate",
+            }
+        ]})
+        _write(cycle / "skill_recommendations.json", {"skill_recommendations": ["Improve grounded repair targeting"]})
+
+    monkeypatch.setattr(
+        "ops.docgen.universal_overlay.self_improvement_handoff.write_training_material_index",
+        lambda output_path=None, root=None: {
+            "record_count": 3,
+            "by_slot": {"slot14": 1, "slot32": 2, "slot120": 0},
+            "training_tracks": {"slot14": 1, "slot32": 2, "slot120": 0},
+        },
+    )
+
+    outputs = create_self_improvement_handoff(
+        run_dir=run_dir,
+        document_type="policy_framework",
+    )
+
+    manifest = json.loads(outputs["training_queue_manifest"].read_text())
+    agent_queue = json.loads(outputs["agent_training_queue"].read_text())
+    model_queue = json.loads(outputs["model_training_queue"].read_text())
+    stability_queue = json.loads(outputs["stability_queue"].read_text())
+    skill_validation_queue = json.loads(outputs["skill_validation_queue"].read_text())
+    scope_payload = json.loads(outputs["skill_scope_decisions"].read_text())
+
+    assert manifest["queue_counts"]["agent_training_queue"] >= 2
+    assert manifest["queue_counts"]["model_training_queue"] == 3
+    assert manifest["queue_counts"]["stability_queue"] == 1
+    assert manifest["queue_counts"]["skill_validation_queue"] == 15
+    assert any(item["track"] == "slot14" for item in agent_queue)
+    assert any(item["track"] == "slot32" for item in agent_queue)
+    assert len(model_queue) == 3
+    assert len(skill_validation_queue) == 15
+    assert skill_validation_queue[0]["requires_approval"] is False
+    assert stability_queue[0]["queue_type"] == "stability_queue"
+    assert manifest["training_tracks"]["slot32"] == 2
+    assert scope_payload["summary"]["shared_candidates"] >= 1
+
+
+def test_handoff_disabled_preserves_failure_evidence_without_training(tmp_path):
+    run_dir = tmp_path / "run"
+    cycle = run_dir / "cycle_01"
+    cycle.mkdir(parents=True)
+    (cycle / "draft.md").write_text("# Policy Framework", encoding="utf-8")
+    _write(cycle / "metrics.json", {"overall_score": 0.91, "changes_applied": []})
+    _write(cycle / "document_profile_conformance.json", {"status": "REVIEW"})
+    _write(cycle / "skills_report.json", {"hard_gate_allowed": False})
+    _write(cycle / "training_quarantine.json", {"hard_gate_failures": ["gap"]})
+    _write(cycle / "repair_plan.json", {"selected_count": 2})
+    _write(cycle / "section_failure_diagnostics.json", {"failures": []})
+
+    outputs = create_self_improvement_handoff(
+        run_dir=run_dir,
+        document_type="policy_framework",
+        export_training_intake=True,
+        self_improvement_enabled=False,
+    )
+
+    readiness = json.loads(outputs["self_improvement_readiness"].read_text())
+    manifest = json.loads(outputs["training_queue_manifest"].read_text())
+    ledger = json.loads(outputs["problem_ledger"].read_text())
+    skills = json.loads(outputs["skill_candidate_requests"].read_text())
+
+    assert readiness["decision"] == "GENERATION_ONLY"
+    assert readiness["autonomous_self_improvement_ready"] is False
+    assert readiness["governance"]["self_improvement_enabled"] is False
+    assert manifest["status"] == "SELF_IMPROVEMENT_DISABLED"
+    assert manifest["queue_counts"]["skill_validation_queue"] == 0
+    assert ledger["problem_count"] >= 1
+    assert skills["request_count"] >= 0
diff --git a/ops/tests/test_logi_week1_foundation.py b/ops/tests/test_logi_week1_foundation.py
index 7726ca7..2aa5011 100644
--- a/ops/tests/test_logi_week1_foundation.py
+++ b/ops/tests/test_logi_week1_foundation.py
@@ -83,7 +83,8 @@ class TestProjectStateManager:
 
         # Training should now be runnable
         updated_training = await manager.get_task(training.task_id)
-        assert not updated_training.depends_on  # dependency removed
+        assert repair.task_id in updated_training.depends_on  # lineage preserved
+        assert not updated_training.blocked_by  # live gate cleared
         assert updated_training.is_runnable()
 
         await manager.disconnect()
