FINAL_STATUS: PARTIAL_P1_GOVERNANCE_AND_SOURCE_VERSIONING_ONLY

The first governed repair slice is implemented and tested. Direct-main autonomous commits are denied by the new contract, and real source version/closeout replay covers all 142 raw records. The P1 target verdict is intentionally not claimed because canonical LearningUnit extraction, semantic routing, versioned clearance authority, persisted route stores, runtime cycles and automatic repair remain incomplete.
