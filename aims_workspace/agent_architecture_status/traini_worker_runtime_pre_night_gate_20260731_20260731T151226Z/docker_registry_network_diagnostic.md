# docker_registry_network_diagnostic

```json
{
  "registry": "docker.io/nvidia/cuda:13.0.0-base-ubuntu24.04",
  "ipv4_curl": "HTTP 401 from registry endpoint (reachable)",
  "ipv6_curl": "connection failed",
  "dns": "registry-1.docker.io resolved IPv4 addresses; auth.docker.io resolved IPv4/IPv6",
  "daemon_proxy": "not configured in docker info output",
  "static_ip_hosts_change": false,
  "diagnosis": "Initial bounded build timed out during slow layer transfer; retry completed successfully. No permanent network repair was used."
}

```
