# P1 active session migration / Redis runtime cycle

Current status: `BLOCKED_ACTIVE_SESSION_MIGRATION`. Stale reconciliation and verified repair routing are complete in bounded evidence-only mode. Active session migration and Redis-owned runtime remain blocked to avoid unsafe process termination or fabricated proof.
